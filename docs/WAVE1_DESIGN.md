# voicx Wave 1 — authoritative chat-encryption design

I have everything I need. All load-bearing claims verified against the code. Key findings that decide the disputes:

- `Migrate()` (store.go:57-84) has **no ledger** — every `.sql` re-execs on every boot. Destructive DML in a migration file is a permanent booby trap. Root fix: add the ledger first.
- `Authenticate.PublicKey`/`AuthSignature.PublicKey` are **Ed25519**, not X25519 — the critique's proposed MOTD fix doesn't work as stated, but adding an X25519 field solves it better than either design.
- `handleChatPins` (chatx.go:594-616) has **no membership check** — confirmed, in neither matrix.
- `files` has `UNIQUE (channel_id, name)` + version rotation — per-file keys break historical attachments (S6 real).
- `renderBody` splits on a **1-capture** regex with stride 2 — Design B's 2-capture change corrupts rendering and leaks the key into `innerHTML`.
- `handleChatSend` reaches `chatKeys.current(attackerChannelID)` before any membership check or rate limit — confirmed mint-DoS.

```json
{
  "summary": "Channel/global chat becomes ciphertext-at-rest on every path (store, history, pins, edits, search, announcements, MOTD, attachment bytes). The server stays a scope-key holder because moderation (114-119) requires it; the threat model this buys is precisely DB READ access — dumps, backups, WAL, replicas, DBA/hosting access, SQL injection, log leakage — and explicitly NOT live process compromise and NOT DB write access.\n\nEleven decisions, each resolving a conflict between the input designs by reading the code:\n\n1. A REAL MIGRATION LEDGER LANDS FIRST. internal/store/store.go:57-84 re-execs every .sql on every boot (verified; its own doc comment admits it). Both input designs put destructive DML in 012 anyway, which would wipe the MOTD on every restart forever. `schema_migrations` is added to Migrate() as prerequisite work; only then is 012 allowed to contain a DELETE.\n2. LEGACY PLAINTEXT IS ENCRYPTED IN PLACE, NOT DESTROYED (Design A wins). The server can read those rows right now, therefore it can seal them. Design B's 'the wave-4b keys are gone' argument is irrelevant — we mint a fresh generation and seal under it. Destroying all history is an enormous, avoidable user cost. `chat_legacy_history=purge` exists for operators who prefer destruction. The dead-tuple/WAL/base-backup caveat is logged loudly with exact remediation.\n3. MINTING IS NEVER A SIDE EFFECT OF A LOOKUP. handlers.go:840-851 reaches chatKeys.current(attacker-supplied channelID) before any membership check and before the rate limiter (chatx.go:163). With persistence that is a disk-exhaustion DoS. `current()` is now non-minting and returns ErrNoScopeKey; only `EnsureScope` mints, and it is reachable only from deliverScopeKey (client provably in the scope), rotate, and one explicit boot-time mint of the global scope.\n4. GENERATION IDs COME FROM chat_scope_seq, NEVER FROM RAM (Design B wins). Design A's rotate() derives id from an in-RAM map that rotateScopeKey can reach unloaded (deliverScopeKey returns early at e2e.go:93 when E2EPublicKey==\"\"), minting a colliding 'generation 1' that orphans the entire legacy backfill. DB-allocated ids make that class impossible.\n5. GENERATIONS ARE NEVER DELETED. Design B's `used=false -> DELETE` races the store path (INSERT commits, then rotate deletes, then markUsed no-ops) and silently orphans messages undetectably. Retire-only. Rows are ~120 bytes; growth is bounded by the rotation coalescing window instead.\n6. KEYS RIDE ALONG WITH HISTORY/PINS RESPONSES (Design B wins). Design A's separate MsgChatKeys round-trip per page costs 10-20 serialized RTTs through the process-global reqMu (client/conn.go:418-441), which is also shared with MsgFileTransferInit that attachFileRef fires per image. Piggyback costs zero extra round trips. Types 99/100 survive only as a pull path for live broadcasts and edits.\n7. MOTD IS SEALED IN AuthResponse, NOT MOVED TO A LATER FRAME. Design B's MsgServerMessage is broken: Connect() (client/conn.go:296-315) sets m.motd synchronously and returns, publishE2EKey is a bare fire-and-forget write, and main.js:232 calls onConnect() -> App.MOTD() immediately — a one-shot read that would always see \"\". Resolution: add X25519PublicKey to Authenticate/AuthSignature (the existing PublicKey field is Ed25519 — verified — so it cannot be sealed to), and return the sealed global scope key plus the sealed MOTD in AuthResponse itself. Client decrypts inside Connect() before returning. Zero frontend change, zero race, zero new ordering rule.\n8. THE ATTACHMENT REGEX KEEPS EXACTLY ONE CAPTURE GROUP. Design B's two-group regex with the loops left at `i += 2` (chat-ui.js:272-292 splits with stride 1+captures) would feed the base64 file key through renderMarkdown into innerHTML — corrupting rendering and violating its own 'the key is never written into the DOM' invariant. The token stays `[file:<one capture>]` and is parsed inside attachFileRef.\n9. CHAT ATTACHMENTS ARE CONTENT-ADDRESSED ON DISK. files has UNIQUE (channel_id, name) and finalizeUpload rotates same-name files through .v1..v3 (verified). With per-file random keys a second `image.png` would hard-fail Poly1305 on every older message referencing it. Storage name becomes sha256(ciphertext)[:32]+\".vcx\"; the display name lives only in the encrypted body.\n10. chat_allow_plaintext IS NARROWED TO 'ACCEPT PLAINTEXT SENDS'. It never affects storage, never affects the relay (the server seals before broadcasting so the wire format is uniform), and never affects history/MOTD. The client refuses any history entry that arrives with a non-empty Body. Both the relay hole and the server-controlled downgrade hole are closed.\n11. PROVENANCE IS AN EXPLICIT CLIENT-SET FLAG. Stripping ciphertext at the Go boundary (both designs) leaves renderMsg (chat-ui.js:222-231, badge only `if (m.e2e || m.enc)`) drawing NO badge on history — identical to today, which is the exact honesty gap in the brief. The client Go layer sets enc_verified after a successful open; normalize and renderMsg key off it.\n\nAlso fixed, found by reading the code rather than the brief: handleChatPins (chatx.go:594-616) has NO membership check at all — any authenticated client, guest included, can read the pinned bodies of any channel it has never joined. That is a worse leak than matrix item 7 and appears in neither input matrix. handleChatEdit (chatx.go:427-484) never validates msg.KeyID before calling open(), so it silently fails on a rotated key.\n\nScope: 2 new message types (99/100), 1 migration (012) plus a ledger retrofit, 1 new server package (internal/chatcrypto), 4 parallel work packets.",

  "key_lifecycle": "## What a generation is\n\n`(scope_id, key_id, key[32], kek_id, created_at, retired_at)`. scope_id 0 = global, else channels.id. Exactly one row per scope has `retired_at IS NULL` (enforced by a partial unique index).\n\n## 1. ID allocation — DB, never RAM\n\n```sql\nINSERT INTO chat_scope_seq (scope_id, next_key_id) VALUES ($1, 2)\nON CONFLICT (scope_id) DO UPDATE SET next_key_id = chat_scope_seq.next_key_id + 1\nRETURNING next_key_id - 1;\n```\n\nThis is the single most important change versus Design A. Today `rotate` computes `prev.id + 1` from `m.keys[scope]` (chatkeys.go:68-79) and `prev` is nil whenever the scope was never loaded. `rotateScopeKey` fires from tcp.go:634 on EVERY disconnect and handlers.go:1045 on EVERY channel switch, while the only loader is `current()` via `sealFor` — which `deliverScopeKey` skips entirely when the client never published an X25519 key (e2e.go:92-95). So one old client joining and leaving a channel mints a colliding \"generation 1\" whose random bytes differ from the persisted gen 1 that the legacy backfill used. DB-allocated ids make an id physically unreusable even if a row is deleted.\n\n## 2. Minting — three call sites, all authorised\n\n```go\nvar ErrNoScopeKey = errors.New(\"no chat key generation for this scope\")\n\n// EnsureScope returns the scope's current generation, MINTING generation 1\n// when the scope has none. It is the ONLY minting entry point.\nfunc (m *chatKeyManager) EnsureScope(ctx context.Context, scope int64) (uint32, [32]byte, error)\n\n// current returns the scope's current generation and NEVER mints.\n// Returns ErrNoScopeKey when the scope has no generation.\nfunc (m *chatKeyManager) current(ctx context.Context, scope int64) (uint32, [32]byte, error)\n```\n\n`EnsureScope` is reachable from exactly three places:\n- `cmd/server/main.go`, once at boot, for `globalChatScope` only. Global is a fixed known scope; minting it eagerly is safe and removes the only legitimate lazy-mint on a hot path.\n- `deliverScopeKey(client, scope)` — the caller has already established that this client is IN that scope (`handleKeyPublish` uses `sc.ChannelID`; `moveClientTo` uses the channel it just joined).\n- `rotate(scope)` — only ever called for a channel that had members.\n\nEverything else (`handleChatSend`'s key-id validation, `open`, `seal`, history assembly) calls `current`/`at`, which never mint. `handleChatSend` on ErrNoScopeKey replies `errCodeUnavailable, \"no chat key for this channel yet — rejoin the channel\"`.\n\nDefence in depth: `handleChatSend` also gains the membership check it lacks today, BEFORE it touches chatKeys:\n\n```go\nif msg.Enc && msg.ChannelID != \"\" {\n    channelID, err := strconv.ParseInt(msg.ChannelID, 10, 64)\n    if err != nil { return s.sendError(client, errCodeMalformed, \"invalid channel_id: \"+msg.ChannelID) }\n    sc, ok := s.deps.State.GetClient(client.ID)\n    if !ok || sc.ChannelID != channelID {\n        return s.sendError(client, errCodePermissionDenied, \"not a member of this channel\")\n    }\n    // ... only now the key-id check, with the non-minting current()\n}\n```\n\n`crypto/rand` failure is fatal, never swallowed. chatkeys.go:55-59 currently returns an all-zero key on rand failure and chatkeys.go:136 does `_, _ = rand.Read(nonce[:])`, yielding a fixed zero nonce. With persistence a zero key is unrecoverable corruption and a zero nonce reused across the entire legacy backfill under one key is a catastrophic secretbox break. Both now return errors that callers surface as `errCodeUnavailable`.\n\n## 3. Protection at rest — internal/chatcrypto\n\n```go\npackage chatcrypto\n\ntype KEKRing struct { keys map[uint16][32]byte; newest uint16 }\n\n// LoadKEKRing loads the key-encryption-key ring from envB64 (if non-empty)\n// or from path. allowCreate is true ONLY when the database holds zero scope\n// key rows; otherwise a missing ring is a fatal startup error.\nfunc LoadKEKRing(path, envB64 string, allowCreate bool) (*KEKRing, error)\nfunc (r *KEKRing) Wrap(key [32]byte) (kekID uint16, wrapped []byte, err error)\nfunc (r *KEKRing) Unwrap(kekID uint16, wrapped []byte) ([32]byte, error)\nfunc (r *KEKRing) NewestID() uint16\n// Fingerprint is hex(sha256(\"voicx-kek-fp-v1\" || newestKey))[:16], domain-\n// separated. Logged at startup. NOT exposed on /health or any unauthenticated\n// surface — it is an oracle over the master key, however weak.\nfunc (r *KEKRing) Fingerprint() string\n```\n\nWrapped form: `nonce[24] || secretbox(key32, KEK)` — 72 bytes, stored BYTEA.\n\nFile format: one `<decimal id>:<base64 of exactly 32 raw bytes>` line per KEK, mode 0600, created with `crypto/rand` when `allowCreate`. `VOICX_CHAT_MASTER_KEY` holds the same format (a bare base64 32-byte value with no `id:` prefix is accepted as id 1).\n\n**There is deliberately no passphrase mode.** Design A's `sha256(chat_key_secret)` is an unsalted, unstretched hash of operator-chosen text; `chat_key_secret=companyname2024` is dictionary-recoverable from exactly the DB dump this design exists to defend against. Anything that is not 32 raw bytes base64 is rejected at load with `chat master key must be 32 raw bytes, base64-encoded (generate with: openssl rand -base64 32)`.\n\n**Fail-closed.** `cmd/server/main.go` calls `store.CountScopeKeys(ctx)` before loading the ring and passes `allowCreate = (count == 0)`. With rows present and no ring, the server refuses to start:\n\n```\nchat master key missing: <n> scope key generations cannot be unwrapped.\nrestore <path> (or set VOICX_CHAT_MASTER_KEY) — it is backed up WITH the database.\nto abandon all chat history instead, run with --reset-chat-keys.\n```\n\nSilently minting a fresh ring would orphan every stored message. This guard is the single best decision in either input design and is kept verbatim in spirit.\n\nKEK rotation: append a higher id; new wraps use `newest`, `Unwrap` dispatches on the row's `kek_id`. `voicx-server rewrap-chat-keys` re-wraps every row under the newest and is the only thing that ever rewrites `wrapped_key`.\n\n## 4. Manager shape and locking\n\n```go\ntype scopeEntry struct {\n    mu     sync.Mutex          // guards this scope's DB I/O and fields\n    loaded bool\n    curID  uint32\n    curKey [32]byte\n    old    map[uint32][32]byte // archival generations, capped at 64 per scope\n    oldLRU []uint32            // eviction order\n}\n\ntype chatKeyManager struct {\n    mu     sync.Mutex               // guards `scopes` map lookup ONLY — never held across DB I/O\n    scopes map[int64]*scopeEntry\n    store  ScopeKeyStore\n    kek    *chatcrypto.KEKRing\n    logger *zap.Logger\n}\n\nfunc newChatKeyManager(st ScopeKeyStore, kek *chatcrypto.KEKRing, log *zap.Logger) *chatKeyManager\nfunc (m *chatKeyManager) EnsureScope(ctx context.Context, scope int64) (uint32, [32]byte, error)\nfunc (m *chatKeyManager) current(ctx context.Context, scope int64) (uint32, [32]byte, error)\nfunc (m *chatKeyManager) at(ctx context.Context, scope int64, gen uint32) ([32]byte, error)\nfunc (m *chatKeyManager) rotate(ctx context.Context, scope int64) (uint32, [32]byte, error)\nfunc (m *chatKeyManager) sealFor(ctx context.Context, scope int64, gen uint32, memberPubB64 string) (*netproto.ChannelKey, error)\nfunc (m *chatKeyManager) open(ctx context.Context, scope int64, gen uint32, blobB64 string) (string, error)\nfunc (m *chatKeyManager) seal(ctx context.Context, scope int64, plain string) (uint32, string, error)\n```\n\nThe two-level lock is mandatory, not stylistic. Design B holds one process-global `sync.Mutex` across DB calls, which would serialise every chat send server-wide behind one lock plus a network round trip. Here `m.mu` is held only to find-or-create the `*scopeEntry`; all DB I/O happens under that entry's own mutex. Different scopes never contend.\n\n`rotate` must NOT redistribute keys while holding any lock — `deliverScopeKey -> sealFor -> current` would re-enter. `rotate` returns; `rotateScopeKey` then loops the members. This is today's structure (e2e.go:121-126) and it is now an explicit documented invariant with a comment at both ends.\n\n## 5. Rotation\n\nTrigger unchanged: member leave (tcp.go:634 disconnect, handlers.go:1045 move-out), channel scope only, global never rotates.\n\n```go\nfunc (m *chatKeyManager) rotate(ctx, scope) (uint32, [32]byte, error) {\n    // under the scope entry's mutex:\n    //   1. loadOrMint from the store (NEVER trust RAM for the id)\n    //   2. id := store.AllocScopeKeyID(ctx, scope)\n    //   3. key := crypto/rand 32 bytes (error is fatal)\n    //   4. kekID, wrapped := kek.Wrap(key)\n    //   5. store.RotateScopeKey(ctx, scope, id, wrapped, kekID)  // ONE transaction:\n    //        UPDATE chat_scope_keys SET retired_at = NOW() WHERE scope_id=$1 AND retired_at IS NULL;\n    //        INSERT INTO chat_scope_keys (...) VALUES (...);\n    //   6. move the outgoing generation into entry.old, install the new one\n}\n```\n\nGenerations are **never deleted**. Design B's `used = false -> DELETE` optimisation is dropped: `StoreChatMessage` and `markUsed` are separate statements, so an INSERT can commit between the used-check and the DELETE, leaving a row referencing a generation that no longer exists — satisfying every CHECK constraint and rendering `[encrypted message — key unavailable]` forever, undetectably. No optimisation is worth a silent-data-loss race.\n\nGrowth is bounded instead by **coalescing**: `rotate` is a no-op when the current generation is younger than `chat_key_rotate_min_seconds` (default 60) AND no member left in between is still absent — concretely, `rotateScopeKey` sets a per-scope dirty flag and a 60s timer; the timer fires one rotation covering all leaves in the window. A client flapping on a bad link produces one generation per minute, not one per reconnect. At 120 bytes/row that is ~63 MB per channel per century. Combined with decision 3 (no attacker-driven minting) the table cannot be made to grow adversarially.\n\n## 6. Distribution — three routes\n\n- **Push, current generation** (unchanged): `handleKeyPublish` (e2e.go:45-48) and `rotateScopeKey`, via `MsgChannelKey`, sealed `box.SealAnonymous` to the member's X25519 key.\n- **Auth response** (new): `AuthResponse.ChatKeys` carries the global generation sealed to `Authenticate.X25519PublicKey`, so a client can decrypt the MOTD before `Connect()` returns.\n- **Piggyback on reads** (new, the main history path): `ChatHistoryResponse.Keys` / `ChatPinsResponse.Keys` carry exactly the distinct generations that page references, sealed for the caller. Zero extra round trips.\n- **Pull** (new, types 99/100): `MsgChatKeyRequest` / `MsgChatKeyBundle`, used only for live broadcasts and `chat_edited` events referencing a generation the client missed. Capped at 64 ids per request; the response sets `Truncated` when it could not serve all of them, and the client re-requests the remainder. `Refused` means \"deliberately withheld\" and is a distinct, permanent state. A client MUST distinguish truncation from refusal — conflating them (Design A's `Missing`-only response with a silent 64 cap) turns a transient truncation into a permanent \"[missing key]\" on screen.\n\n`handleChatKeyRequest` is rate-limited by the existing `chatRate` limiter keyed on unique ID, because an empty-ids request is cheap to send and expensive to answer (one `box.SealAnonymous` per generation).\n\n## 7. Entitlement\n\nOne predicate, used by history, pins, search and key requests alike:\n\n```go\n// scopeReadable reports whether the client may read a scope's messages.\n// Identical to the check handleChatHistory already performs (chatx.go:373-378).\nfunc (s *TCPServer) scopeReadable(client *Client, scope int64) bool {\n    if scope == globalChatScope { return true }\n    sc, ok := s.deps.State.GetClient(client.ID)\n    return ok && sc.ChannelID == scope\n}\n```\n\nA current member of a scope may fetch every generation of that scope. This is **not** a widening: today the server hands the same member the full plaintext scrollback of that channel on request. Refusing older generations while still serving their rows would be theatre. There is no persistent channel-membership table in the schema, so \"membership at message time\" is information the server does not have.\n\nThis is stated out loud in the chatkeys.go header and in the shield tooltip, because \"rotate on leave\" reads like forward secrecy and is not: rotation stops an ex-member reading FUTURE messages and does nothing about history they were entitled to.\n\n`chat_history_scope=since_join` is **not built**. Both input designs floated it; it needs a grants table, a first-join hook, guest semantics, and a story for `moveClientTo`, and it delivers a property (history windowing) that no backlog item asks for. Recorded here as a deliberate omission so it is not re-litigated.\n\nOne hard gate added: a client that has never published an X25519 key cannot read history at all — `handleChatHistory` and `handleChatPins` return `errCodePermissionDenied, \"publish an encryption key before reading history\"` rather than returning rows it provably cannot decrypt.\n\n## 8. Restart\n\n`newChatKeyManager` loads nothing at boot; `cmd/server/main.go` calls `EnsureScope(ctx, globalChatScope)` once. Everything else loads on first touch, per scope, under that scope's own mutex. Boot stays O(1). Ciphertext written before the restart opens because the key bytes are identical — which also fixes a latent bug that exists today: chatkeys.go:52-61 mints a DIFFERENT random key under the SAME id 1 after every restart, so \"generation 1\" is ambiguous across process lifetimes.\n\n## 9. What a client needs to decrypt an arbitrary old message\n\n1. Its X25519 private key — persisted in `identity.json` (client/identity.go:22-45, survives restarts and reinstalls).\n2. The sealed `(scope, key_id)` generation — from `AuthResponse.ChatKeys`, a history/pins `Keys` array, `MsgChatKeyBundle`, or a join/rotate push.\n3. Membership of the scope at the moment of asking.\n\nNothing else. Losing `identity.json` does not orphan history: generations are sealed on demand to whatever public key the user currently publishes. The only thing lost with the identity is the unique ID itself.",

  "schema_changes": "## PREREQUISITE — the migration ledger (internal/store/store.go)\n\nThis lands **before** 012 and is not optional. Verified: `Migrate()` reads every embedded `.sql` and `db.Exec`s it on every call, sorted lexically, with no version table. Its own doc comment says \"Migrations are idempotent (use CREATE TABLE IF NOT EXISTS) so re-running is safe.\" Both input designs then put an unguarded `UPDATE`/`DELETE` in 012, which would re-run at every boot — Design B's step 7 would wipe the operator's MOTD on every single restart, forever.\n\n```go\n// Migrate applies every embedded migration exactly once, recording each in\n// schema_migrations. Files applied before this ledger existed are re-applied\n// once and then recorded; every migration up to 011 is idempotent DDL, so\n// that single replay is safe. From 012 onward a migration MAY contain\n// destructive DML, because the ledger guarantees it runs exactly once.\nfunc (s *Store) Migrate() error {\n    if _, err := s.db.Exec(`CREATE TABLE IF NOT EXISTS schema_migrations (\n        filename   TEXT PRIMARY KEY,\n        applied_at TIMESTAMPTZ NOT NULL DEFAULT NOW()\n    )`); err != nil {\n        return fmt.Errorf(\"creating schema_migrations: %w\", err)\n    }\n    applied := map[string]bool{} // SELECT filename FROM schema_migrations\n    // ... existing ReadDir + sort.Strings(files) ...\n    for _, name := range files {\n        if applied[name] { continue }\n        content, err := migrationsFS.ReadFile(\"migrations/\" + name)\n        if err != nil { return fmt.Errorf(\"reading migration %s: %w\", name, err) }\n        tx, err := s.db.Begin()\n        if err != nil { return err }\n        if _, err := tx.Exec(string(content)); err != nil {\n            _ = tx.Rollback()\n            return fmt.Errorf(\"applying migration %s: %w\", name, err)\n        }\n        if _, err := tx.Exec(`INSERT INTO schema_migrations (filename) VALUES ($1)`, name); err != nil {\n            _ = tx.Rollback()\n            return fmt.Errorf(\"recording migration %s: %w\", name, err)\n        }\n        if err := tx.Commit(); err != nil { return fmt.Errorf(\"committing migration %s: %w\", name, err) }\n        if s.logger != nil { s.logger.Info(\"migration applied\", zap.String(\"file\", name)) }\n    }\n    return nil\n}\n```\n\nThe explicit transaction also fixes a latent problem: lib/pq sends a multi-statement string as one simple-query batch inside an implicit transaction, so a mid-file failure already rolls back — but the ledger INSERT must be in the SAME transaction as the file, or a crash between them re-runs a destructive migration.\n\n## NEW FILE internal/store/migrations/012_chat_encryption.sql\n\n011_client_custom.sql exists (verified), so 012 is next — the brief's \"next is 011\" is stale and both input designs caught this correctly.\n\n```sql\n-- 012_chat_encryption.sql — ciphertext at rest for channel/global chat\n-- (backlog 91-135, \"all msg must be encrypted\").\n--\n-- Bodies move from chat_messages.body (plaintext TEXT, 007_chat.sql:17) to\n-- body_enc = base64(nonce[24] || secretbox(plain, scope_key[key_id])).\n-- The legacy column survives one release, pinned empty by a CHECK, so the\n-- Go backfill can read it and so operators get one release of audit room.\n--\n-- Applied EXACTLY ONCE via the schema_migrations ledger, so the destructive\n-- statements below are safe. Do NOT add destructive DML to migrations 001-011:\n-- those predate the ledger and are still replayed once on the upgrade boot.\n\n-- 1. Persistent scope key generations ---------------------------------------\n-- Without these, every stored ciphertext becomes unreadable at the next\n-- restart or rotation. wrapped_key is nonce[24] || secretbox(key32, KEK);\n-- the KEK is NEVER in this database (chat_master_key_file / VOICX_CHAT_MASTER_KEY).\nCREATE TABLE IF NOT EXISTS chat_scope_keys (\n    scope_id    BIGINT      NOT NULL,               -- 0 = global, else channels.id\n    key_id      BIGINT      NOT NULL,               -- generation; never reused\n    wrapped_key BYTEA       NOT NULL,\n    kek_id      SMALLINT    NOT NULL DEFAULT 1,\n    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    retired_at  TIMESTAMPTZ,                        -- NULL = current generation\n    PRIMARY KEY (scope_id, key_id)\n);\nCREATE UNIQUE INDEX IF NOT EXISTS uq_chat_scope_keys_current\n    ON chat_scope_keys (scope_id) WHERE retired_at IS NULL;\n\n-- Generation ids are allocated here, never as MAX(key_id)+1 and never from\n-- an in-RAM counter, so a generation id can never be re-minted with\n-- different key material.\nCREATE TABLE IF NOT EXISTS chat_scope_seq (\n    scope_id    BIGINT PRIMARY KEY,\n    next_key_id BIGINT NOT NULL DEFAULT 1\n);\n\n-- 2. chat_messages: ciphertext columns --------------------------------------\nALTER TABLE chat_messages\n    ADD COLUMN IF NOT EXISTS body_enc TEXT   NOT NULL DEFAULT '',\n    ADD COLUMN IF NOT EXISTS key_id   BIGINT NOT NULL DEFAULT 0;\n\n-- 3. server_settings: motd/announcement carry a generation ------------------\n-- Values are NOT blanked here. The Go backfill seals the existing plaintext\n-- in place, exactly as it does for chat bodies — blanking would silently\n-- destroy operator copy on upgrade for no security gain.\nALTER TABLE server_settings\n    ADD COLUMN IF NOT EXISTS key_id BIGINT NOT NULL DEFAULT 0;\n\n-- 4. files: mark client-encrypted chat attachments --------------------------\nALTER TABLE files\n    ADD COLUMN IF NOT EXISTS encrypted BOOLEAN NOT NULL DEFAULT FALSE;\n\n-- 5. Pre-4b plaintext spool rows --------------------------------------------\n-- deliverSpooled (handlers.go:967-978) treats from_unique_id='' rows as\n-- plaintext and replays them as chat. They predate E2EE and cannot be\n-- re-sealed (no recipient key at rest). Undelivered ones are dropped.\n-- Safe because the ledger runs this file exactly once.\nDELETE FROM offline_messages WHERE from_unique_id = '' AND delivered_at IS NULL;\n\n-- 6. Structural bans on plaintext -------------------------------------------\n-- NOT VALID: existing rows still hold plaintext until the Go backfill runs.\n-- New/updated rows are checked immediately, which is the point: from this\n-- moment no code path can write a plaintext body, present or future.\n-- The backfill VALIDATEs both constraints when it finishes.\nDO $$\nBEGIN\n    IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = 'chat_messages_no_plaintext') THEN\n        ALTER TABLE chat_messages\n            ADD CONSTRAINT chat_messages_no_plaintext CHECK (body = '') NOT VALID;\n    END IF;\n    IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = 'chat_messages_sealed') THEN\n        ALTER TABLE chat_messages\n            ADD CONSTRAINT chat_messages_sealed\n            CHECK (deleted_at IS NOT NULL OR (body_enc <> '' AND key_id > 0)) NOT VALID;\n    END IF;\n    IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = 'offline_messages_sealed') THEN\n        ALTER TABLE offline_messages\n            ADD CONSTRAINT offline_messages_sealed\n            CHECK (from_unique_id <> '' OR message = '');\n    END IF;\nEND $$;\n```\n\nOrdering note: the `offline_messages` DELETE (step 5) precedes its CHECK (step 6). Design B had these reversed, and `ADD CONSTRAINT` validates existing rows immediately, so any deployment carrying one pre-4b spool row would fail the migration and the server would not start.\n\n`chat_messages_sealed` is `NOT VALID` for a reason Design B missed: rows with `body = ''` and `deleted_at IS NULL` already exist in production. `handleChatSend` only checks `len(msg.Text) > maxChatBytes` (handlers.go:832), so an encrypted message whose plaintext is the empty string stores exactly that row. The JS will not send one; the protocol will. A validated-on-add constraint would abort the migration on those rows.\n\n## EXISTING PLAINTEXT ROWS — encrypted in place, in Go, once\n\nDefault `chat_legacy_history=encrypt`. New file `internal/server/chatmigrate.go`:\n\n```go\n// EncryptLegacyChatHistory converts pre-012 plaintext bodies to ciphertext in\n// place. It runs once, before the listener binds, and is fatal on error: a\n// partially-encrypted table behind a NOT VALID constraint is exactly the\n// silent-plaintext state the requirement forbids. Idempotent and resumable\n// by id.\nfunc (s *TCPServer) EncryptLegacyChatHistory(ctx context.Context, mode string) error\n```\n\nThe brief asserts encrypt-in-place is impossible server-side without the keys. It is possible and it is the right call: the server is, by the documented trust model (chatkeys.go:1-11), a holder of the channel/global scope keys, and those rows are readable by it *right now*. It mints generation 1 for each scope via `EnsureScope` and seals the legacy rows under it. Members receive that generation through the ordinary delivery paths and history survives the upgrade. Design B's \"the wave-4b keys are gone so we must destroy the text\" conflates *the key that originally sealed the transit ciphertext* (irrelevant — the DB holds plaintext, not that ciphertext) with *a key to seal it under now* (which we mint).\n\nAlgorithm, mode `encrypt`:\n1. If `server_settings['chat_ciphertext_backfill_at']` is set, assert `CountPlaintextBodies(ctx) == 0` and return.\n2. Page `SELECT id, channel_id, body FROM chat_messages WHERE body_enc = '' AND deleted_at IS NULL ORDER BY id LIMIT 500`.\n   **The predicate is `body_enc = ''`, not `body <> ''`.** This is what catches the empty-plaintext rows described above; sealing the empty string yields a perfectly valid non-empty ciphertext and satisfies `chat_messages_sealed`.\n3. Per row: `keyID, ct, err := s.chatKeys.seal(ctx, row.ChannelID, row.Body)` then `SetChatCiphertext(ctx, id, ct, keyID)` = `UPDATE chat_messages SET body_enc=$1, key_id=$2, body='' WHERE id=$3`.\n4. Tombstoned rows (`deleted_at IS NOT NULL`): `UPDATE ... SET body='', body_enc='', key_id=0`.\n5. Seal `server_settings` `motd` and `announcement` under the global generation, writing `(value, key_id)`.\n6. `ALTER TABLE chat_messages VALIDATE CONSTRAINT chat_messages_no_plaintext` and `... VALIDATE CONSTRAINT chat_messages_sealed`, then stamp the marker.\n\nMode `purge`: one `UPDATE chat_messages SET body='', body_enc='', key_id=0, deleted_at=COALESCE(deleted_at, NOW())` for operators who would rather lose history than have it re-sealed under a server-minted key, then the same VALIDATE + marker.\n\n## New store functions\n\n```go\n// internal/store/chat.go\nfunc (s *Store) LegacyPlaintextPage(ctx context.Context, afterID int64, limit int) ([]LegacyChatRow, error)\nfunc (s *Store) SetChatCiphertext(ctx context.Context, id int64, bodyEnc string, keyID uint32) error\nfunc (s *Store) PurgeLegacyPlaintext(ctx context.Context) (int64, error)\nfunc (s *Store) CountPlaintextBodies(ctx context.Context) (int64, error)\nfunc (s *Store) ValidateChatNoPlaintext(ctx context.Context) error\n\n// internal/store/chatkeys.go (new file)\ntype ScopeKey struct { KeyID uint32; Wrapped []byte; KEKID uint16; CreatedAt time.Time; RetiredAt *time.Time }\nfunc (s *Store) CountScopeKeys(ctx context.Context) (int64, error)\nfunc (s *Store) AllocScopeKeyID(ctx context.Context, scope int64) (uint32, error)\nfunc (s *Store) CurrentScopeKey(ctx context.Context, scope int64) (*ScopeKey, error)   // nil, nil when none\nfunc (s *Store) GetScopeKey(ctx context.Context, scope int64, keyID uint32) (*ScopeKey, error)\nfunc (s *Store) InsertScopeKey(ctx context.Context, scope int64, keyID uint32, wrapped []byte, kekID uint16) error\nfunc (s *Store) RotateScopeKey(ctx context.Context, scope int64, newKeyID uint32, wrapped []byte, kekID uint16) error // retire+insert in ONE tx\n```\n\n`InsertScopeKey` is a **hard error on primary-key conflict** — never `ON CONFLICT DO NOTHING`. A conflict means two different key materials are competing for one generation id, which is a corruption signal that must be loud.\n\n## HONEST LIMIT — logged at WARN on the backfill boot\n\nBlanking a column does not erase the old tuple. Dead tuples, WAL segments and existing base backups still contain every plaintext body. The log line reads:\n\n```\nlegacy chat history encrypted in place (N rows). THE OLD PLAINTEXT IS NOT GONE:\nrun VACUUM (FULL, ANALYZE) chat_messages, then rotate or destroy every base\nbackup and WAL archive taken before this upgrade.\n```\n\n## DEFERRED to migration 013, next wave\n\n`ALTER TABLE chat_messages DROP COLUMN body`. Keeping the permanently-empty, CHECK-guarded column for one release lets an operator verify `SELECT count(*) FROM chat_messages WHERE body <> ''` = 0 before it disappears.\n\n**Rollback is NOT possible and the release notes say so in the first line.** Design A claimed the retained column gives \"one release of rollback room\"; it does not. A `NOT VALID` CHECK is still enforced against every new INSERT and UPDATE, and the pre-012 binary's `StoreChatMessage` (store/chat.go:34-35) inserts into `body`. A rolled-back binary violates the constraint on every message — and `routeScopedChat` swallows the error (`s.logger.Warn(\"storing chat message failed\")`, chatx.go:223-228) and relays anyway, so a mixed-version deployment would keep chat working while writing zero history at WARN level indefinitely. **012 is a one-way door.** Mandatory `pg_dump` before upgrading, stated as step 1 of the release notes.",

  "protocol_changes": "All in `internal/netproto/codec.go`. Highest existing type is 98 (`MsgServerInfoResponse`, codec.go:130) — verified.\n\n## New message types — two, not three\n\n```go\n\tMsgChatKeyRequest MessageType = 99  // client -> server: request specific scope key generations\n\tMsgChatKeyBundle  MessageType = 100 // server -> client: sealed generations (+ refusals)\n```\n\nPlus the two `String()` cases (\"ChatKeyRequest\", \"ChatKeyBundle\") and the two `Decode` registry cases.\n\nDesign B's third type (`MsgServerMessage` for MOTD) is **not** added — see motd_announcement_plan. It solved an ordering problem that is better solved by sealing into `AuthResponse`, and as specified it broke the MOTD entirely.\n\n## New structs\n\n```go\n// ChatKeyRequest asks for specific scope chat key generations the client is\n// missing: a live broadcast or chat_edited event referencing a generation it\n// never received. History and pins do NOT use this — they carry their keys\n// inline (see ChatHistoryResponse.Keys), because a nested request/response\n// would serialise behind the client's process-global request mutex.\n// KeyIDs is capped at 64 per request; an empty list is rejected.\ntype ChatKeyRequest struct {\n\tChannelID int64    `json:\"channel_id\"`\n\tKeyIDs    []uint32 `json:\"key_ids\"`\n}\n\n// ChatKeyBundle answers a ChatKeyRequest. Keys holds the generations the\n// caller is entitled to, each sealed with box.SealAnonymous to the caller's\n// published X25519 key — the same envelope as ChannelKey.\n//\n// The three outcomes are distinct and MUST NOT be conflated by clients:\n//   - present in Keys  -> usable\n//   - present in Refused -> permanently withheld (not a member, or unknown\n//     generation). Render \"you do not have access\".\n//   - absent from both, with Truncated set -> the server capped the response.\n//     Render \"key unavailable\" and re-request. Treating truncation as\n//     refusal turns a transient cap into a permanent \"[missing key]\".\ntype ChatKeyBundle struct {\n\tChannelID int64        `json:\"channel_id\"`\n\tKeys      []ChannelKey `json:\"keys\"`\n\tRefused   []uint32     `json:\"refused,omitempty\"`\n\tTruncated bool         `json:\"truncated,omitempty\"`\n}\n```\n\n## Changed structs\n\n```go\n// ChatHistoryEntry — codec.go:824-833.\ntype ChatHistoryEntry struct {\n\tID           int64  `json:\"id\"`\n\tFromUniqueID string `json:\"from_unique_id\"`\n\tFromNickname string `json:\"from_nickname\"`\n\n\t// BodyEnc is the stored ciphertext:\n\t// base64(nonce[24] || secretbox(plain, scopeKey[KeyID])).\n\t// The SERVER populates ONLY BodyEnc/KeyID and NEVER Body — including when\n\t// chat_allow_plaintext is set. There is no server-controlled switch that\n\t// makes history plaintext on the wire.\n\tBodyEnc string `json:\"body_enc,omitempty\"`\n\tKeyID   uint32 `json:\"key_id,omitempty\"`\n\n\t// Body is the DECRYPTED text. It is ALWAYS empty on the wire. The Wails\n\t// Go layer (client/chat.go) fills it after unsealing, before the webview\n\t// sees the entry. A non-empty Body arriving from a server is a protocol\n\t// violation and the client replaces the entry with a refusal string.\n\tBody string `json:\"body,omitempty\"`\n\n\t// EncVerified is set by the CLIENT after it successfully opens BodyEnc.\n\t// It never appears on the wire from a server. It exists so the renderer\n\t// can draw the shield on a message replayed from history — without it,\n\t// renderMsg's `if (m.e2e || m.enc)` draws nothing and a history message\n\t// is visually indistinguishable from one the server handed over in the\n\t// clear, which is the exact honesty gap this wave must close.\n\tEncVerified bool `json:\"enc_verified,omitempty\"`\n\n\tSentAt    int64          `json:\"sent_at\"`\n\tEditedAt  int64          `json:\"edited_at,omitempty\"`\n\tDeleted   bool           `json:\"deleted,omitempty\"`\n\tReactions map[string]int `json:\"reactions,omitempty\"`\n}\n\n// ChatHistoryResponse — codec.go:836-839.\ntype ChatHistoryResponse struct {\n\tChannelID int64              `json:\"channel_id\"`\n\tMessages  []ChatHistoryEntry `json:\"messages\"`\n\t// Keys carries exactly the distinct generations this page references,\n\t// sealed for the caller. Refused/Truncated as in ChatKeyBundle.\n\tKeys      []ChannelKey `json:\"keys,omitempty\"`\n\tRefused   []uint32     `json:\"refused,omitempty\"`\n\tTruncated bool         `json:\"truncated,omitempty\"`\n}\n\n// ChatPinsResponse — codec.go:876-879: same three fields added.\n// ChatPinEntry is unchanged; it embeds *ChatHistoryEntry and inherits the fix.\n\n// Authenticate — the existing PublicKey field is the Ed25519 IDENTITY key\n// (it feeds auth.UniqueIDFromPublicKey and auth.VerifyChallenge) and cannot\n// be sealed to. This is the X25519 encryption key, the same value\n// MsgKeyPublish carries, supplied at auth time so the server can seal the\n// global scope key and the MOTD into AuthResponse.\n\tX25519PublicKey string `json:\"x25519_public_key,omitempty\"`\n\n// AuthSignature — same field, for the guest/challenge path, which does not\n// populate Authenticate.PublicKey.\n\tX25519PublicKey string `json:\"x25519_public_key,omitempty\"`\n\n// AuthResponse — codec.go:365-384.\n\t// MOTD is the server's message of the day, SEALED under the global scope\n\t// generation MOTDKeyID when MOTDEnc is set. Empty when unset or when the\n\t// client supplied no X25519 key.\n\tMOTD      string `json:\"motd,omitempty\"`\n\tMOTDEnc   bool   `json:\"motd_enc,omitempty\"`\n\tMOTDKeyID uint32 `json:\"motd_key_id,omitempty\"`\n\t// ChatKeys carries the global scope generation (and nothing else) sealed\n\t// to X25519PublicKey, so the client can open MOTD before Connect()\n\t// returns. Channel keys still arrive via MsgChannelKey after key publish.\n\tChatKeys []ChannelKey `json:\"chat_keys,omitempty\"`\n\n// FileTransferInitResponse — codec.go:614-618.\n\t// TLS reports that the file-transfer port requires a TLS handshake, and\n\t// TLSFingerprint is the SHA-256 of its certificate — the SAME certificate\n\t// as the control channel, so the client re-uses the pin it already holds.\n\tTLS            bool   `json:\"tls,omitempty\"`\n\tTLSFingerprint string `json:\"tls_fingerprint,omitempty\"`\n```\n\n## Event payload changes (JSON maps, not structs)\n\n- `announcement`: `{\"text\": <base64 ciphertext>, \"enc\": true, \"key_id\": <global gen>}` (was `{\"text\": <plaintext>}`, chatx.go:861-863).\n- `chat_edited`: shape unchanged. `body` now carries the client's original ciphertext verbatim instead of a server re-seal, and `key_id` is the generation the client sealed under.\n\n## Unchanged but now load-bearing\n\n`ChatSend.Enc/KeyID`, `ChatBroadcast.Enc/KeyID/E2E`, `ChatEdit.Enc/KeyID`, `ChannelKey`. Nothing about the live wire format moves.\n\n## Explicitly NOT changed, and why\n\n`ServerInfoResponse.MOTD` stays a plaintext string. `handleServerInfoQuery` answers `MsgServerInfoQuery` for callers holding no scope key; there is no key under which it could be sealed without deleting the feature. New config `server_info_motd` (default true) lets an operator omit it entirely. See motd_announcement_plan.\n\n`Poke.Message`, `SetStatus.Message` and `Complaint.Reason` are user-authored text that is still relayed and stored in the clear. They are **out of scope for this wave** and listed as known plaintext paths in the README trust model rather than half-fixed. Design B added `Enc` to `Poke` (client->server) but not to `pokeEvent` (server->client), which would have rendered raw base64 into the recipient's poke toast — a strictly worse outcome than leaving it alone.",

  "server_changes": "## NEW internal/chatcrypto/chatcrypto.go\n`KEKRing`, `LoadKEKRing`, `Wrap`, `Unwrap`, `NewestID`, `Fingerprint` exactly as in key_lifecycle. ~130 lines. Depends only on `crypto/rand`, `crypto/sha256`, `encoding/base64`, `golang.org/x/crypto/nacl/secretbox`. A separate package so `rewrap-chat-keys` and the tests can use it without importing `internal/server`.\n\n## internal/server/chatkeys.go — rewritten\nThe two-level lock, the three-call-site minting rule, DB-allocated ids, retire-only rotation, and the coalescing timer, all as specified in key_lifecycle. The file header comment is rewritten: the current \"Keys live in memory only ... which also bounds history readability to the server's uptime\" paragraph becomes false and is replaced with the entitlement statement (a current member may fetch every generation of that scope; rotation is not forward secrecy over history).\n\n`open` gains an explicit `gen` parameter. Today it always uses `current()` (chatkeys.go:115-128), which is why `handleChatEdit` silently fails after a rotation.\n\n## internal/server/chatx.go\n\n- **`spamTracker` (:88-121).** `spamEntry.body string` becomes `sum [32]byte`. `record(uid string, sum [32]byte, now time.Time) bool`. The heuristic is unchanged — `record` only ever compares bodies for exact equality (`e.body == body`, :115), so a hash is a drop-in. Callers pass `sha256.Sum256([]byte(stripAttachmentRefs(plain)))`.\n  Note for the implementer: `sha256.Sum256(x)[:16]` does **not compile** — you cannot slice the unaddressable array a function returns. Assign to a temporary first. (This exact expression appears in Design B and would have failed the build.)\n\n- **`stripAttachmentRefs(body string) string`** (new, ~6 lines). Replaces `[file:<storage>#<key>#<display>]` with `[file:<display>]` before `moderateBody` and before `spamTracker.record`. Without it every image post carries fresh random base64, every post is unique, and the \"same message 3x in 30s\" heuristic (116) stops catching image spam. The word filter still sees the display name.\n\n- **`routeScopedChat` (:159-262).** After the moderation pipeline, store the sender's ORIGINAL ciphertext verbatim:\n  ```go\n  bodyEnc, keyID := msg.Text, msg.KeyID\n  if !msg.Enc { // chat_allow_plaintext: the server seals before storing AND before relaying\n      keyID, bodyEnc, err = s.chatKeys.seal(ctx, channelID, plain)\n      if err != nil { return s.sendError(client, errCodeUnavailable, \"chat key unavailable\") }\n  }\n  id, err := s.deps.Chat.StoreChatMessage(ctx, channelID, uid, client.Username, bodyEnc, keyID)\n  ```\n  `handleChatSend` has already proved `msg.KeyID == current`, so the bytes that were moderated are exactly the bytes that are stored — no code path where the two can diverge.\n\n  **The storage error stops being swallowed.** Today `s.logger.Warn(\"storing chat message failed\")` and the relay proceeds (:223-228). With a CHECK constraint in play that turns a constraint violation into invisible, indefinite history loss. It now returns `errCodeUnavailable, \"message not stored — not delivered\"` and does NOT relay, so a broken storage path is a visible outage rather than a silent one.\n\n  **The relay is now always encrypted.** The broadcast uses `bodyEnc`/`keyID`/`Enc: true` rather than `msg.Text`/`msg.Enc`. Today with `chat_allow_plaintext` on, `Enc: msg.Enc` (:239) fans a plaintext body out to every client in the scope. Under \"all msg must be encrypted\" that is a live config-controlled hole; sealing the relay closes it and makes the wire format uniform.\n\n- **`chatHistoryEntry` (:401-418).** `e.BodyEnc = m.BodyEnc; e.KeyID = m.KeyID`; `e.Body` is never assigned. Tombstones set `BodyEnc=\"\"`, `KeyID=0`, `Deleted=true`. This one function fixes matrix leaks 6 and 7 — `ChatPinEntry` embeds `*ChatHistoryEntry` (verified), so pins inherit it.\n\n- **`handleChatHistory` (:365-398).** Keeps its membership check, adds the published-key gate, and assembles the key bundle:\n  ```go\n  gens := map[uint32]bool{}\n  for _, m := range msgs { if m.KeyID != 0 { gens[m.KeyID] = true } }\n  for gen := range gens {\n      if len(resp.Keys) >= maxKeysPerResponse { resp.Truncated = true; break }\n      ck, err := s.chatKeys.sealFor(ctx, msg.ChannelID, gen, sc.E2EPublicKey)\n      if err != nil { resp.Refused = append(resp.Refused, gen); continue }\n      resp.Keys = append(resp.Keys, *ck)\n  }\n  ```\n  `maxKeysPerResponse = 64`. Truncation is reported, never silently converted to \"missing\".\n\n- **`handleChatPins` (:594-616).** **Add the membership check it does not have.** Verified: it decodes, checks `s.deps.Chat != nil`, and calls `ChatPins(ctx, msg.ChannelID)` with no gate whatsoever — any authenticated client, guest included, can read the pinned bodies, message ids, authors and timestamps of any channel it has never joined. Compare `handleChatHistory` (:373-378), which does gate. Fix directly with `scopeReadable`; do not let key-gating become accidental access control (that would close the body leak while leaving the metadata enumeration open). Then the same key-bundle assembly as history.\n\n- **`handleChatEdit` (:427-484).** Add the missing key-id validation (`msg.KeyID` must equal `current(stored.ChannelID)`, mirroring handleChatSend — today it is absent, so an edit under a rotated key just fails to decrypt with a confusing error). Store `msg.NewText`/`msg.KeyID` verbatim. Broadcast `msg.NewText` as-is; the re-seal at :472-474 becomes dead code for the encrypted path.\n\n- **NEW `handleChatKeyRequest`** (type 99): decode, reject empty `KeyIDs`, cap at 64, run `s.chatRate.allow(uid, now)` (this handler is cheap to call and expensive to serve), `scopeReadable` gate, then `sealFor` per generation; unknown or ungranted generations go into `Refused`.\n\n- **`SetServerSettingAndAnnounce` (:854-865).** Seal under the current global generation, store `(value, key_id)`, broadcast `{\"text\": ct, \"enc\": true, \"key_id\": gen}`.\n\n- **NEW `serverSettingSealed(ctx, key) (blob string, gen uint32, err error)`** — reads the row and, if `key_id` names a retired generation, re-seals under the current one and writes it back, so nobody is ever locked out of the MOTD.\n\n## internal/server/e2e.go\n- `deliverScopeKey` calls `EnsureScope` (this is an authorised mint site) and passes the generation to `sealFor`.\n- `rotateScopeKey` becomes context-aware, applies the coalescing window, and handles `rotate`'s error by logging and NOT rotating — a stale key is far better than an unreadable channel. A comment at both ends records the invariant that redistribution happens strictly after `rotate` returns, because `deliverScopeKey -> sealFor -> current` re-enters the scope entry's mutex.\n- NEW `scopeReadable(client, scope) bool`.\n\n## internal/server/handlers.go\n- `handleAuthenticate` (~:414): capture `msg.X25519PublicKey` (or `AuthSignature.X25519PublicKey` on the challenge path), call `s.deps.State.SetE2EPublicKey(client.ID, x25519)` immediately so the key is live before the snapshot, then populate `AuthResponse.ChatKeys` with the sealed global generation and `MOTD`/`MOTDEnc`/`MOTDKeyID` with the sealed MOTD. Clients that send no X25519 key get `ChatKeys` empty and `MOTD` empty (or plaintext under `chat_allow_plaintext`).\n- `:428-434`: the stored-announcement replay STAYS here, unchanged in position. Design B moved it into `handleKeyPublish`; that drops it for every client that never publishes a key, because `deliverScopeKey` returns early at e2e.go:93 — a functional regression neither design listed. It works in place because the client now holds the global generation from `AuthResponse.ChatKeys` before the announcement arrives.\n- `handleChatSend` (:822-897): the membership check described in key_lifecycle, then the non-minting `current()`, then `ErrNoScopeKey -> errCodeUnavailable`.\n- `deliverSpooled` (:967-978): drop the `e2e := m.FromUniqueID != \"\"` legacy branch; `Enc`/`E2E` become unconditionally true. Migration 012 removed the undelivered legacy rows and the CHECK prevents new ones.\n\n## internal/server/admin.go\n`SendServerText` (:131-166) — verified plaintext today: it builds `netproto.ChatBroadcast{From: \"ServerQuery\", Text: msg}` with `Enc` unset, so `maybeDecryptChat` passes it straight through to the renderer. It now seals: the channel generation for targetmode 2, the global generation for modes 1 and 3, with `Enc: true, KeyID: n`. Mode 1 (\"direct to one client\") is sealed under the GLOBAL key and the doc comment states plainly that a ServerQuery direct message is a server notice delivered to one client, not an E2EE DM — the server has no DM key and never did.\n\n## internal/server/deps.go\n- `ChatStore`: `StoreChatMessage(ctx, channelID int64, fromUniqueID, fromNickname, bodyEnc string, keyID uint32) (int64, error)`, `EditChatMessage(ctx, id int64, bodyEnc string, keyID uint32) error`, `SetServerSetting(ctx, key, value string, keyID uint32) error`, `GetServerSetting(ctx, key string) (string, uint32, error)`, plus the five backfill helpers.\n- NEW `ScopeKeyStore` interface (the seven methods from schema_changes), added to `Deps` as `ScopeKeys`.\n\n## internal/server/tcp.go\nDispatch `netproto.MsgChatKeyRequest -> s.handleChatKeyRequest`. `NewTCPServer` takes the `*chatcrypto.KEKRing` and builds `newChatKeyManager(deps.ScopeKeys, kek, logger)`. The `rotateScopeKey` call at :634 gets a context.\n\n## NEW internal/server/chatmigrate.go\n`EncryptLegacyChatHistory` per schema_changes.\n\n## internal/config/config.go\n```go\nChatMasterKeyFile     string `mapstructure:\"chat_master_key_file\"`      // \"./data/keys/chat_master.key\"\nChatLegacyHistory     string `mapstructure:\"chat_legacy_history\"`       // \"encrypt\" (default) | \"purge\"\nChatKeyRotateMinSecs  int    `mapstructure:\"chat_key_rotate_min_seconds\"` // 60\nChatSearchMaxMessages int    `mapstructure:\"chat_search_max_messages\"` // 2000\nFileTLSEnabled        bool   `mapstructure:\"file_tls_enabled\"`         // true\nServerInfoMOTD        bool   `mapstructure:\"server_info_motd\"`         // true\n```\n`chat_allow_plaintext` keeps its `false` default; its docstring becomes: \"permits unencrypted chat SENDS (dev only). It never affects storage, relay, history or MOTD — the server seals before storing and before broadcasting regardless.\"\n\n## cmd/server/main.go\nBoot order, all fatal on error:\n`store.New` -> `store.Migrate()` (ledger + 012) -> `n := store.CountScopeKeys(ctx)` -> `chatcrypto.LoadKEKRing(cfg.ChatMasterKeyFile, os.Getenv(\"VOICX_CHAT_MASTER_KEY\"), n == 0)` -> log fingerprint + \"BACK THIS UP WITH THE DATABASE\" -> `NewTCPServer(...)` -> `tcp.EnsureGlobalScopeKey(ctx)` -> `tcp.EncryptLegacyChatHistory(ctx, cfg.ChatLegacyHistory)` -> filetransfer with TLS material from the SAME `tlscert.Ensure` call the control channel uses -> `tcp.Start()`.\nNew subcommands: `rewrap-chat-keys`, and `--reset-chat-keys` (truncates `chat_scope_keys`/`chat_scope_seq`, tombstones all chat, and requires an interactive confirmation).",

  "client_changes": "## client/e2e.go\n\n- **`scopeKeyStore.put` must be split — this is a prerequisite, not a cleanup.** Verified at :75-83: `put` sets `s.latest[scope] = id` unconditionally, `current()` reads `latest`, `encryptChat` seals with `current()`, and handlers.go:848 rejects a stale key id. Installing an ARCHIVAL generation from a history page would push `latest` backwards and every subsequent send would be refused with \"stale chat key for channel\". Both input designs caught this and both were right.\n  ```go\n  // put installs the scope's CURRENT generation, advancing latest only\n  // forward. An out-of-order delivery must never downgrade the send key.\n  func (s *scopeKeyStore) put(scope int64, id uint32, key [32]byte) // if id > s.latest[scope] { s.latest[scope] = id }\n  // putGen installs an ARCHIVAL generation for history/pins/search. It never\n  // touches latest.\n  func (s *scopeKeyStore) putGen(scope int64, id uint32, key [32]byte)\n  func (s *scopeKeyStore) has(scope int64, id uint32) bool\n  ```\n  `handleChannelKey` and `AuthResponse.ChatKeys` route to `put`; history/pins/bundle route to `putGen`.\n\n- `func (m *connManager) installKeys(scope int64, keys []netproto.ChannelKey)` — `box.OpenAnonymous` each and `putGen`. Shared by history, pins, search and the bundle handler.\n- `func (m *connManager) requestKeys(scope int64, ids []uint32) error` — writes `MsgChatKeyRequest`. Fire-and-forget; the reply lands on the read loop.\n- Two placeholders replace the single `missingKeyText` (:30), because the difference matters to the user:\n  ```go\n  const missingKeyText = \"[encrypted message — key unavailable]\"   // transient: retry pending\n  const refusedKeyText = \"[encrypted message — you do not have access]\" // permanent\n  const refusedPlainText = \"[refused: server sent plaintext history]\"\n  ```\n- `maybeDecryptChat` (:287-319): the missing-key branch at :310-312 currently gives up immediately. It now fires `go guardCrash(\"chatKeyFetch\", func() { m.fetchKeyAndRetry(chat, payload) })`.\n  **This goroutine is mandatory and so is the guard.** `maybeDecryptChat` runs ON the read loop (conn.go:485-490), and `connManager.request` holds a process-global `reqMu` while waiting for a reply that arrives on that same loop — so any synchronous key fetch from an event handler self-deadlocks. The existing DM branch already spawns a goroutine for exactly this reason and says so at :300-307. `guardCrash` is required because this parses attacker-controlled ciphertext off the read loop (item 331); `recover` is per-goroutine.\n- Generalised to `maybeDecryptEvent`, keeping the `chat` branch verbatim and adding:\n  - `announcement`: unseal `data.text` with `scopeKeys.get(0, data.key_id)`.\n  - **`chat_edited`**: unseal `data.body`. This deletes a real workaround rather than taxing it — chat-ui.js:948-961 currently re-fetches an entire history page just to read the edited text, because `maybeDecryptChat` returns early unless `env.Type === \"chat\"` (:292). That workaround also silently fails when the edited message is more than PAGE messages back. One extra branch here removes it.\n- `sealFile(data []byte, key [32]byte) ([]byte, error)` / `openFile(blob []byte, key [32]byte) ([]byte, error)` — `nonce[24] || secretbox`, raw bytes, no base64.\n\n## client/chat.go\n\n- `ChatHistory` (:27-38) decrypts before returning to the webview:\n  ```go\n  m.installKeys(channelID, resp.Keys)\n  refused := map[uint32]bool{}\n  for _, g := range resp.Refused { refused[g] = true }\n  for i := range resp.Messages {\n      e := &resp.Messages[i]\n      if e.Deleted { continue }\n      if e.BodyEnc == \"\" && e.Body != \"\" { e.Body = refusedPlainText; e.EncVerified = false; continue }\n      key, ok := m.scopeKeys.get(channelID, e.KeyID)\n      switch {\n      case refused[e.KeyID]: e.Body = refusedKeyText\n      case !ok:              e.Body = missingKeyText\n      default:\n          if p, err := openScope(e.BodyEnc, key); err != nil { e.Body = missingKeyText } else { e.Body, e.EncVerified = p, true }\n      }\n      e.BodyEnc, e.KeyID = \"\", 0 // the webview NEVER sees ciphertext or key ids\n  }\n  ```\n  The `BodyEnc == \"\" && Body != \"\"` refusal is the client-side half of closing the downgrade hole: no server config can make this client render a plaintext history body. `EncVerified` is the provenance flag that makes the shield honest on replayed messages.\n- `ChatPins` (:79-90): identical treatment over `resp.Pins[i].Message`.\n- **NEW `ChatSearch(channelID int64, query string, maxMessages int) ([]netproto.ChatHistoryEntry, error)`** — see search_plan.\n- `ChatEditMessage` (:43-58): unchanged; it already seals with the current scope key.\n- **Attachments.** Note both return shapes are `(T, error)`, matching every other bound method in `client/` — Design A's `(string, string)` signature would have had the frontend's `await` silently discard the error half.\n  ```go\n  // UploadChatAttachment seals data with a fresh random 32-byte key, uploads\n  // the ciphertext under a content-derived name, and returns the body token\n  // \"[file:<storage>#<keyB64>#<display>]\" for embedding in the chat message.\n  func (a *App) UploadChatAttachment(channelID int64, name, dataBase64 string) (string, error)\n  // DownloadChatAttachment fetches <storage> and unseals it with keyB64,\n  // returning base64 plaintext. An empty keyB64 downloads a plain file.\n  func (a *App) DownloadChatAttachment(channelID int64, storage, keyB64 string) (string, error)\n  ```\n  Storage name is `hex(sha256(ciphertext))[:32] + \".vcx\"`. **This is what makes per-file keys viable at all.** `files` has `UNIQUE (channel_id, name)` (003_files.sql, verified) and `finalizeUpload` rotates a same-name file through `.v1..v3`; with per-file random keys, a second `image.png` would leave every older message holding a key that fails Poly1305 against the new blob — a hard decryption failure where today there is a soft overwrite. Screenshot paste produces colliding names (`image.png`, `Clipboard.png`) constantly. Content-addressing removes name collisions, version rotation and the failure mode together.\n  The display name has `]` and `#` replaced with `_` before it enters the token.\n- `ftDial` (:245-249) — the \"plaintext this wave\" comment goes away:\n  ```go\n  func ftDial(addr, fingerprint string) (net.Conn, error) {\n      return tls.DialWithDialer(&net.Dialer{Timeout: 15 * time.Second}, \"tcp\", addr, &tls.Config{\n          InsecureSkipVerify:    true, // pinned below — same TOFU model as client/tofu.go\n          VerifyPeerCertificate: pinFingerprint(fingerprint),\n      })\n  }\n  ```\n  The fingerprint comes from `FileTransferInitResponse.TLSFingerprint`, cross-checked against the control-channel fingerprint the client pinned at conn.go:141. A mismatch aborts, so a hostile server cannot redirect uploads to a third-party host.\n- `ExportChat` (:364-378): keeps the plaintext path behind a frontend confirm; NEW `ExportChatEncrypted(defaultName, contents, passphrase string) (string, error)` writes `magic || argon2id-salt || nonce || secretbox(contents)` as `.voicxchat`, mode 0600. Argon2id is already available — both modules require `golang.org/x/crypto v0.54.0` and `internal/auth/password.go` already imports it (verified).\n\n## client/conn.go\n\n- `connect`/`connectWith`: set `X25519PublicKey` on `Authenticate` (password path) **and** on `AuthSignature` (guest/challenge path — verified that the guest path leaves `Authenticate.PublicKey` empty and supplies the Ed25519 key via the signer callback, so both structs need the field).\n- `Connect()` (~:296-315): after `resp.OK`, install `resp.ChatKeys` via `put`, then decrypt `resp.MOTD` when `resp.MOTDEnc` and assign the plaintext to `m.motd` — **all before `Connect()` returns.** This is what keeps `App.MOTD()` (app.go:169-175) a correct one-shot read and `main.js:232 -> onConnect() -> await app().MOTD()` unchanged.\n- Read-loop switch (~:480-511): `case netproto.MsgChatKeyBundle` -> `installKeys` + wake any pending retry.\n- `MsgChannelKey` (:491) routes to `put`; everything archival routes to `putGen`.\n\n## client/frontend/src/chat-ui.js\n\n- `normalize` (:75-99): `enc: !!d.enc || !!d.enc_verified`. One token. Without it, `renderMsg`'s `if (m.e2e || m.enc)` (:222-231) draws **no badge at all** on a history message — indistinguishable from a plaintext one, which is precisely the honesty gap the brief names.\n- `renderMsg` (:222-231): shield tooltip becomes \"encrypted with this channel's key — stored encrypted. The server holds the channel key so it can moderate at send time; it does not keep the text.\" DM lock unchanged (it was already accurate). New `⚠` badge + `missing-key` class when the text is one of the three placeholders.\n- **`renderBody` (:272-292): the regex keeps EXACTLY ONE capture group and both loops stay at `i += 2`.**\n  ```js\n  const parts = text.split(/\\[file:([^\\]]+)\\]/);   // UNCHANGED\n  for (let i = 0; i < parts.length; i += 2) html += renderMarkdown(parts[i]);\n  container.innerHTML = applyCustomEmoji(html);\n  for (let i = 1; i < parts.length; i += 2) attachFileRef(container, m, parts[i]);\n  ```\n  `String.prototype.split` with a capturing regex interleaves captures at stride `1 + captureCount`. Design B's two-group regex with the loops left at `i += 2` would feed `parts[2]` — the base64 **file key** — through `renderMarkdown` into `innerHTML`, and pass body text to `attachFileRef` as a filename: a rendering corruption and a direct violation of its own \"the key is never written into the DOM\" invariant. Parsing inside `attachFileRef` avoids the whole class.\n- NEW `parseFileRef(cap)` — total, never throws, legacy-compatible:\n  ```js\n  function parseFileRef(cap) {\n      const i = cap.indexOf(\"#\");\n      if (i < 0) return { storage: cap, key: \"\", name: cap };          // legacy [file:photo.png]\n      const j = cap.indexOf(\"#\", i + 1);\n      if (j < 0) return { storage: cap, key: \"\", name: cap };          // malformed -> treat as plain\n      return { storage: cap.slice(0, i), key: cap.slice(i + 1, j), name: cap.slice(j + 1) };\n  }\n  ```\n- `attachFileRef` (:296-338): takes the raw capture, calls `parseFileRef`, and uses `app().DownloadChatAttachment(chID, ref.storage, ref.key)`. Display uses `ref.name`. The key never enters the DOM (no `textContent`, no attribute, no `src`).\n- Attachment send (:1012-1020): `const token = await app().UploadChatAttachment(chID, f.name, f.dataBase64)` then `SendChat(scope, target, token)`.\n- `searchServer` (:1387-1417): the 10x50 JS paging loop is deleted, replaced by one `await app().ChatSearch(chID, q, MAX)`. `applySearchFilter`, `showSearchResults` and jump-to-message are untouched. The results header reports \"N matches (M messages could not be decrypted)\".\n- `exportChat` (:1545-1555): confirm dialog (\"this writes DECRYPTED messages to a plain file on your disk\") plus an \"encrypt with a passphrase\" option calling `ExportChatEncrypted`.\n- `onConnect` (:1590-1601): unchanged in mechanics. The MOTD line becomes `V().sysMsg(\"server notice — \" + motd)` and is styled as a system banner rather than a chat message, so nothing that is not a message ever renders next to a shield or a lock.\n- `onAnnouncement` (:1561-1588): unchanged — `d.text` is decrypted in Go before the event is emitted, so the banner, the `hashStr(text)` dismiss key and the notification matrix all keep working.\n- The edit workaround at :948-961 (re-fetch a history page to read an edited body) is DELETED; `chat_edited` now arrives decrypted.\n- NEW: a short \"How chat encryption works here\" block in the channel info panel (`openDescription`, :1511-1543), sourced from one exported string so the client and the README cannot drift.\n\n## client/frontend/src/files-ui.js\nRows with `encrypted: true` render as \"🔒 chat attachment\" with download disabled and the tooltip \"open it from the chat message that carries its key\". Every existing `DownloadFile(chID, name)` call site is unaffected (that method stays for the file browser).\n\n## cmd/e2e/main.go\nThe black-box harness asserts `m.Body == text` (:991) and `m.Body != \"\"` (:1077) on `ChatHistoryResponse` — both break under the new wire format, and neither input design's test plan mentioned the one binary that validates the actual protocol end to end. It gains: `X25519PublicKey` at auth, `AuthResponse.ChatKeys` handling, `box.OpenAnonymous` of the piggybacked `Keys`, and `openScope` of `BodyEnc` before the equality assertions.",

  "search_plan": "## What survives, and why it costs almost nothing\n\n`searchServer` (chat-ui.js:1387-1417) never used a server-side search. Verified: `grep -rn \"ChatSearch|ILIKE|to_tsvector\" --include=*.go .` returns nothing, and `internal/query/commands.go` has no chat-search command. The existing implementation pages `ChatHistory` up to 10 x 50 and matches with `(x.body || \"\").toLowerCase().includes(q)` in JavaScript. The only thing ciphertext-at-rest breaks is that `x.body` now arrives sealed — and the Wails Go layer already holds the keys.\n\n## What changes\n\nThe paging loop moves from JS into Go as `App.ChatSearch`:\n\n```go\n// ChatSearch pages the scope's history backwards, decrypting each page with\n// the keys the server bundles alongside it, and returns matching messages\n// newest-first. Search runs entirely client-side: the server stores only\n// ciphertext and cannot match on content (110).\nfunc (a *App) ChatSearch(channelID int64, query string, maxMessages int) (ChatSearchResult, error) {\n    q := strings.ToLower(query)\n    var out []netproto.ChatHistoryEntry\n    undecryptable, before, scanned := 0, int64(0), 0\n    for scanned < maxMessages {\n        resp, err := a.ChatHistory(channelID, before, 200) // decrypts + strips ciphertext\n        if err != nil || len(resp.Messages) == 0 { break }\n        for _, m := range resp.Messages {\n            scanned++\n            if m.Deleted { continue }\n            if !m.EncVerified { undecryptable++; continue }\n            if strings.Contains(strings.ToLower(m.Body), q) { out = append(out, m) }\n        }\n        before = resp.Messages[len(resp.Messages)-1].ID\n        if len(resp.Messages) < 200 { break }\n        wailsRuntime.EventsEmit(a.ctx, \"chatsearch:progress\", scanned)\n    }\n    return ChatSearchResult{Messages: out, Scanned: scanned, Undecryptable: undecryptable}, nil\n}\n```\n\nThree reasons this belongs in Go: the keys live in Go and must not cross into the webview; the page size can use the store's cap of 200 (store/chat.go:50-52) instead of the UI's 50, cutting round trips 4x; and 2000 `secretbox.Open` calls are single-digit milliseconds.\n\n## Why the keys ride along instead of being fetched\n\nThis is the decision that makes the cost model work, and it is where Design A was wrong by an order of magnitude. `connManager.request` (client/conn.go:418-441) holds a **process-global** `reqMu` for the whole send-and-wait, and `m.pending` is keyed by message *type*, so the serialisation is load-bearing rather than incidental. A nested `MsgChatKeys` exchange per page would make a search up to 20 serialised round trips on a mutex shared with avatar fetches, `PermissionsQuery`, `WebRTCOffer`, `EmojiGet` and — critically — `MsgFileTransferInit`, which `attachFileRef` fires once per image while rendering the very pages being searched. On a 150ms link the entire client request path would stall for seconds.\n\nWith keys piggybacked on `ChatHistoryResponse`, a search is exactly `ceil(maxMessages/200)` round trips — 10 by default — and the pull path (99/100) is never touched.\n\n## User-visible behaviour\n\n- Ctrl+F live filtering (`applySearchFilter`, :1350-1362) is completely unchanged — it reads `el.textContent`, already decrypted.\n- \"search server history\" looks and behaves the same. The cap rises from 500 (10 x 50) to a configurable 2000, and a progress counter replaces the static \"searching…\" label.\n- Messages under a generation the client cannot obtain never match and are counted: the header reads \"N matches (M messages could not be decrypted)\", so a partial search never looks complete.\n- DM search is unchanged (client-local, never server-side).\n\n## What is lost, explicitly\n\n- Server-side or admin retrospective grep over stored chat is now impossible **by construction**. It was never built, so nothing regresses today — but the door is closed rather than merely unbuilt, and that is an operator cost for abuse handling and legal response. It belongs in the README trust model, not in a footnote.\n- Any future \"search all channels\" or \"search the entire history\" feature is off the table without downloading and decrypting everything client-side.\n\n## Rejected alternative, recorded so it is not re-derived\n\nA **blind index** — per-scope HMAC of normalised word/trigram tokens in a `chat_message_tokens` table — would restore unbounded server-side prefiltering. Rejected: it leaks token frequency and co-occurrence to exactly the party we just took plaintext away from; it needs its own key generation, rotation and reindex story layered on the one this design already introduces; it does not survive an edit cleanly; and it doubles the write path. If the 2000-message ceiling becomes a real complaint, this is the escape hatch and its price is \"the server learns which messages share which words\". It slots in as a new table without touching anything designed here.",

  "filetransfer_plan": "Two independent problems. Both need fixing; neither substitutes for the other.\n\n## Fix 1 — the port stops being plaintext\n\n`internal/filetransfer/server.go:438` is a bare `net.Listen(\"tcp\", s.cfg.Addr)` with no `crypto/tls` import anywhere in the package (verified), and `client/chat.go:245-246` says so out loud: *\"plaintext this wave; it's a separate port from the TLS control channel.\"*\n\n**The stolen bytes are not the worst part.** The single-use transfer token is issued over TLS on the control channel and then presented in **cleartext** on this port, so an on-path observer can lift it and pull the file itself. That is the sharpest observation in either input design and it is correct.\n\n- `filetransfer.Config` gains `TLSEnabled bool`, `Cert tls.Certificate`, `Fingerprint string`. `Server` exposes `Fingerprint() string`.\n- `Start` wraps the listener — the only line that moves:\n  ```go\n  ln, err := net.Listen(\"tcp\", s.cfg.Addr)\n  if err != nil { return fmt.Errorf(\"filetransfer listen on %s: %w\", s.cfg.Addr, err) }\n  if s.cfg.TLSEnabled {\n      ln = tls.NewListener(ln, &tls.Config{\n          Certificates: []tls.Certificate{s.cfg.Cert},\n          MinVersion:   tls.VersionTLS12,\n      })\n  }\n  ```\n- `cmd/server/main.go` builds `Cert` from the **same** `tlscert.Ensure(cfg.TLSDir, cfg.TLSCertFile, cfg.TLSKeyFile, hosts)` call the control channel already uses (tcp.go:281-285), so there is no second certificate to manage and no new trust decision for the user — it is the certificate they already accepted.\n- `FileTransferInitResponse` carries `TLS: true` and `TLSFingerprint`, so an un-upgraded client gets a clear failure instead of a hung handshake.\n- Client `ftDial(addr, fingerprint)` uses `tls.DialWithDialer` with `InsecureSkipVerify: true` plus a `VerifyPeerCertificate` comparing SHA-256 against the pin, cross-checked against the control-channel fingerprint from conn.go:141.\n- `file_tls_enabled` defaults true; false is a dev-only escape hatch that logs `file transfer listener started (PLAINTEXT!)` in the same style as the control channel's existing warning.\n\nThis also protects the **non-chat** file browser (items 261-267), which had identical exposure.\n\n## Fix 2 — chat attachment bytes are ciphertext on disk\n\nTLS still leaves an image pasted into a channel readable to anyone with server disk access — the same leak class this design removes for message bodies. Chat attachments are therefore sealed client-side, before upload:\n\n1. The client generates a random 32-byte file key K and produces `nonce[24] || secretbox(fileBytes, K)`.\n2. It uploads that ciphertext under a **content-derived name**: `hex(sha256(ciphertext))[:32] + \".vcx\"`.\n3. K rides inside the chat body: `[file:<storage>#<base64 K>#<display name>]`. The body is then sealed under the scope key (or the recipient's box key for a DM) like any other message, so K gets exactly the protection the message text gets. **No new key distribution, no new protocol, no new table.**\n4. `files.encrypted = true` drives the file-browser UI.\n\n### Why content-addressing is mandatory, not a nicety\n\n`files` declares `UNIQUE (channel_id, name)` (003_files.sql) and `finalizeUpload` (internal/filetransfer/transfer.go:162-192) *replaces* a same-name file, rotating the previous blob through `.v1..v3` with `maxFileVersions = 3`. Today a second `image.png` in a channel is a soft collision: older messages render the newer image. With per-file random keys it becomes a **hard** failure — message #1 holds K1 while the blob holds ciphertext under K2, so `openFile` fails Poly1305 and a message that used to work now errors. After the fourth same-name upload the original bytes are gone entirely. Clipboard and screenshot paste (chat-ui.js:1012-1020) produce colliding names constantly. Content-addressing eliminates collisions, version rotation and this failure mode in one move. Neither input design caught this; Design B explicitly claimed lost dedup was the *only* cost.\n\n### Moderation stays intact\n\nThe server sees K because it decrypts the body — consistent with the channel trust model and no worse than today. But `stripAttachmentRefs(body)` runs **before** `moderateBody` and **before** `spamTracker.record`, rewriting the token to `[file:<display name>]`. Without it every image post carries fresh random base64, no two posts are ever equal, and the \"same message 3x in 30s\" heuristic (116) stops catching repeated image spam. The word filter still sees the display name.\n\n### Confidentiality, stated precisely\n\nFor a DM this is true E2EE for the attachment. For a channel it equals scope-key confidentiality: the server could recover K in the instant it moderates, but it stores neither K nor the plaintext bytes. That is exactly the trust model the project already documents; no new promise is made.\n\n**A caveat Design A overclaimed:** it called the DM case \"a strict improvement\" over today. In fact DM attachments are already broken independently of encryption — upload uses `const chID = scope === \"channel\" ? st.myChannelID : 0` (~:1013) while download uses `const chID = m.channelID ?? V().state.myChannelID ?? 0` (~:294), and `normalize` leaves `channelID` null for DMs, so sender and recipient disagree about the channel. This design does **not** fix that id resolution; DM attachments remain broken and are logged as a separate defect rather than silently \"encrypted\".\n\n## Consequences, stated\n\n- **SHA-256 dedup never hits for chat attachments** — a fresh random key means fresh ciphertext, so the same meme costs two copies. File-browser uploads are unaffected and keep dedup.\n- **Expiring download links (267) refuse `.vcx`.** `CreateLink` returns `\"encrypted chat attachment — open it in the client\"`; `/dl/<token>` on the health port has no key and would otherwise serve ciphertext that looks like corruption.\n- **The file browser disables download for `encrypted` rows**, tooltip \"open it from the chat message that carries its key\".\n- **Version rotation no longer applies** to chat attachments (content-addressed names never collide). This is a fix, not a loss.\n- **File-browser uploads stay unencrypted at rest.** The browser is a deliberately shared drive; encrypting it needs a per-channel file key with its own rotation story and breaks links, dedup and versioning. Both paths get TLS; only chat attachments get at-rest encryption. That split is visible in the UI via the 🔒 marker rather than assumed.\n- **File names remain plaintext** in the `files` table for browser uploads, and chat attachment *display* names live only inside the encrypted body — but size, uploader unique ID, channel and timestamp stay plaintext metadata. Called out, not hidden.\n- **The transfer digest now verifies the ciphertext**, which is what is stored. Plaintext integrity comes from secretbox's Poly1305 tag — strictly stronger than the SHA-256 comparison it replaces.\n- `internal/filetransfer` has zero awareness of any of this: it moves opaque bytes, which is the point.",

  "motd_announcement_plan": "## MOTD (133) — sealed, in AuthResponse, with no ordering rule at all\n\nBoth input designs got this wrong in opposite directions. Design A left it plaintext. Design B moved it to a new `MsgServerMessage` sent after key publish and claimed `App.MOTD()` / `motdSnapshot()` / `onConnect()` were \"all unchanged — they read the same field, which is now populated a few frames later.\" **They are a one-shot read, not a subscription.** Verified: `Connect()` (client/conn.go:296-315) sets `m.motd = resp.MOTD`, calls `publishE2EKey()` (a bare `write`, no wait), starts the read loop and returns; `main.js:232` then immediately calls `chatUI.onConnect()`, which does `await app().MOTD()` -> `motdSnapshot()` -> reads `m.motd`. Under Design B that read happens before the server's frame has traversed the network, so the MOTD would essentially never display, and nothing in that design re-renders on late arrival.\n\nThe critique proposed sealing to `Authenticate.PublicKey` instead. That does not work either: **`Authenticate.PublicKey` and `AuthSignature.PublicKey` are the Ed25519 identity key** — verified, they feed `auth.UniqueIDFromPublicKey` (handlers.go:232) and `auth.VerifyChallenge` (:250). `box.SealAnonymous` needs X25519. Converting Ed25519 to X25519 requires the birational map, which is not in the standard library or `x/crypto` and is not a dependency worth adding for this.\n\n**The fix:** add `X25519PublicKey` to `Authenticate` and to `AuthSignature` (both, because the guest/challenge path leaves `Authenticate.PublicKey` empty and supplies its key through the signer callback). The client already has this value at connect time — `id.x25519()` is right there in `identity.go:32-45`. Then:\n\n1. `handleAuthenticate` records the X25519 key in state immediately.\n2. It seals the global scope generation to that key and returns it in `AuthResponse.ChatKeys`.\n3. It seals the MOTD under that same generation and returns `MOTD`/`MOTDEnc`/`MOTDKeyID`.\n4. `Connect()` installs `ChatKeys` and decrypts `MOTD` **before it returns**.\n\nResult: `App.MOTD()`, `motdSnapshot()` and `onConnect()` are genuinely unchanged, there is no race, no new message type, and no new ordering rule anyone can break later. The `MsgKeyPublish` flow continues to exist for channel keys and re-keying.\n\nA client that sends no X25519 key gets an empty MOTD.\n\n### Storage\n`server_settings` gains `key_id`. The MOTD is stored **sealed** under the global generation. Design A argued sealing it at rest is \"theatre\" because the server must re-seal it per client anyway — but that reasoning contradicts the design's own threat model. The entire justification for putting the KEK on the filesystem rather than in Postgres is that a DB dump must not yield plaintext. Operator-authored broadcast text sitting in the clear in that same dump is a hole in the one-line ops audit. Sealing it costs one `secretbox.Seal` on a write that happens perhaps monthly. `serverSettingSealed` re-seals under the current generation if the global key ever rotates and writes the result back, so nobody is locked out.\n\nExisting plaintext values are sealed **in place by the Go backfill**, not blanked. Design B's `UPDATE server_settings SET value = ''` would, without a migration ledger, wipe the operator's MOTD on every single restart — and even with a ledger it destroys operator copy on upgrade for no security gain, since the server can read and re-seal it.\n\n### ServerInfoResponse (313) — the one deliberate plaintext exception\n`handleServerInfoQuery` (presence.go:169-188) answers `MsgServerInfoQuery` for callers that hold no scope key. There is no key under which it could be sealed without deleting the feature. It stays a plaintext string, and this is justified rather than excused: it is operator-authored **public** text, it is never inserted into `chat_messages`, and it is never replayed through the chat path. New config `server_info_motd` (default true) lets an operator omit it entirely.\n\nThe dishonesty to fix here is presentational, not cryptographic. Today the MOTD renders in the chat log looking like a message. It becomes `V().sysMsg(\"server notice — \" + motd)`, styled as a system banner. The invariant the UI can then state truthfully: **everything bearing a lock or a shield came out of ciphertext; anything without one is a local or public notice, not a message.**\n\n## Announcement (132) — sealed, and it stays where it is\n\n`SetServerSettingAndAnnounce` (chatx.go:854-865) currently broadcasts `{\"text\": value}` with no seal. It now seals under the current global generation, stores `(value, key_id)`, and broadcasts `{\"text\": ct, \"enc\": true, \"key_id\": gen}`.\n\nThe stored-announcement replay at `handleAuthenticate` (handlers.go:428-434) **stays exactly where it is.** Design B moved it into `handleKeyPublish`; that would silently drop it for every client that never publishes a key, because `deliverScopeKey` returns early when `sc.E2EPublicKey == \"\"` (e2e.go:93) — a functional regression neither design listed. It works unchanged in place because the client now holds the global generation from `AuthResponse.ChatKeys` before the announcement frame arrives.\n\nClient: `maybeDecryptChat` becomes `maybeDecryptEvent` with an `announcement` branch unsealing `data.text` with `scopeKeys.get(0, data.key_id)`. `chat-ui.js onAnnouncement` (:1561-1588) is unchanged — it keeps receiving plaintext `d.text`, so the banner, the `hashStr(text)` dismiss key and the notification matrix all keep working.\n\n## ServerQuery `sendtextmessage` — same class of leak, fixed here\n\n`SendServerText` (admin.go:131-166) builds `netproto.ChatBroadcast{From: \"ServerQuery\", Text: msg}` with `Enc` unset, so `maybeDecryptChat` passes it through and it renders as plaintext at the application layer. Verified; the audit matrix missed it. It now seals: channel generation for targetmode 2, global generation for modes 1 and 3.\n\nMode 1 is documented rather than pretended: the server has no DM key and never did, so a ServerQuery \"direct message\" is a server notice delivered to one client, readable by any global-key holder who intercepts it. The doc comment and the UI label both say \"server notice\".\n\n## Known, unfixed, and written down\n\n`internal/query/server.go:90` is a bare `net.Listen` with no TLS in the package. `sendtextmessage targetmode=… msg=<text>` crosses that socket in cleartext *before* this design seals the broadcast. Sealing the egress while the ingress is plaintext is half a fix. ServerQuery TLS is **out of scope for this wave** (it is an operator-to-server admin channel, conventionally bound to localhost) and is recorded as an open item in the README trust model with the recommendation to bind it to `127.0.0.1` until it is fixed. `serveredit virtualserver_welcomemessage` (commands.go:649-664) and channel topics/descriptions (008_channel_description.sql) are likewise plaintext operator/user text, listed and not silently ignored.",

  "risks": [
    "LOSING THE KEK DESTROYS ALL CHANNEL AND GLOBAL HISTORY, IRREVERSIBLY. The wrapped scope keys are inert without data/keys/chat_master.key (or VOICX_CHAT_MASTER_KEY). Mitigation: the server REFUSES TO START when chat_scope_keys has rows and the ring cannot be loaded — it must never silently mint a fresh KEK and orphan history; the file is auto-created at 0600 on the same persistent volume as ./data/tls; the fingerprint and 'BACK THIS UP WITH THE DATABASE' are logged at every start; rewrap-chat-keys exists for planned rotation. There is no recovery path by design — that is the property being bought. This is the single largest new operational failure mode and it is entirely a documentation-and-guardrail problem.",
    "012 IS A ONE-WAY DOOR — there is no rollback, and a mixed-version deployment loses history silently. A NOT VALID CHECK still enforces against every new INSERT/UPDATE, and the pre-012 binary inserts into chat_messages.body, so a rolled-back binary violates the constraint on every message. Worse, routeScopedChat currently swallows storage errors (chatx.go:223-228, Warn + relay anyway), so an un-upgraded node against an upgraded database would keep chat working while writing zero history at WARN level indefinitely. Mitigation: this design makes the storage error FATAL to the send (no silent relay), and the release notes state 'take a pg_dump; you cannot roll back' as line one. Design A's claim that the retained body column buys 'one release of rollback room' is false and was removed.",
    "The backfill is fatal-on-error at startup, so a bad upgrade is an outage rather than a degradation. This is intentional — a partially-encrypted table behind a NOT VALID CHECK is exactly the silent-plaintext state the requirement forbids. Mitigation: it is idempotent and resumable by id, pages 500 rows at a time, and chat_legacy_history=purge is the documented escape hatch. Note the VALIDATE CONSTRAINT pass at the end is a full table scan taking an ACCESS EXCLUSIVE lock; on a very large chat_messages this extends the startup outage and the runbook must say so.",
    "BLANKING LEGACY PLAINTEXT DOES NOT ERASE IT. UPDATE leaves dead tuples, and WAL segments plus existing base backups still contain every old body. Mitigation: the backfill logs a WARN with the exact remediation (VACUUM (FULL, ANALYZE) chat_messages, then rotate or destroy every pre-upgrade base backup and WAL archive) and the runbook entry ships with the release. Claiming the data is gone without this would be false.",
    "CONFIDENTIALITY ONLY — NO INTEGRITY BINDING. body_enc is not bound to (id, channel_id, from_unique_id, sent_at); secretbox has no AAD, and the design stores the sender's ciphertext verbatim so binding to a server-assigned message id is impossible without re-sealing. Anyone with DB WRITE access can move one row's (body_enc, key_id) onto another row and both clients decrypt cleanly, attributing the wrong text to the wrong author, undetectably. This is not a regression (the same is true of today's plaintext body), but the threat model must therefore stop precisely at DB READ — dumps, backups, WAL, replicas, DBA/hosting access, SQL injection, log leakage — and the README must not say 'a stolen database is inert' without that qualifier. Adding AAD is a clean follow-up (prefix the sealed plaintext with from_unique_id and have the client verify it) and is deliberately NOT bundled here because it changes the moderated plaintext format.",
    "A COMPROMISED LIVE SERVER STILL READS EVERY CHANNEL MESSAGE. Moderation (114-119) runs on the decrypted body and the hard constraints forbid moving it, so the plaintext exists transiently in server memory during routeScopedChat. This design defends against data at rest, not against process compromise. Mitigation: say so precisely in the README trust model and in the shield tooltip; never let the UI imply 'the server never sees message text'. DMs remain true E2EE and are unaffected. The spamTracker's 30-second plaintext RAM window is removed by hashing, but the transient window is not and cannot be.",
    "HISTORY READABILITY CHANGES SHAPE, AND ROTATION STILL DOES NOT PROTECT HISTORY. Any current member can fetch every generation of their channel — not a widening (today the server hands the same member full plaintext scrollback) but it must be said out loud in the chatkeys.go header and the shield tooltip so nobody reads 'rotate on leave' as forward secrecy. A user who joins and immediately scrolls up decrypts everything; a server that lost a generation renders those messages permanently unreadable. Mitigation: generations are never deleted, only retired.",
    "CHAT NOW DEPENDS ON THE DATABASE FOR KEYS. Previously a DB outage left live chat working on RAM-only keys; now the first touch of each scope after boot needs a SELECT. Mitigation: keys are cached per scope for the process lifetime after that first load, the per-scope mutex means one slow scope cannot stall others, and a failure returns an explicit errCodeUnavailable instead of today's silent zero-key path — which, with ciphertext persisted, would have been unrecoverable corruption.",
    "MULTI-NODE IS A KNOWN LIMITATION, AND WORSE THAN 'STALE READS'. Each node caches the current generation per scope, and uq_chat_scope_keys_current means two nodes rotating the same scope concurrently leaves one INSERT failing the partial unique index. Since rotate logs-and-does-not-rotate on DB failure, that node believes the old generation is current and half the members send under a key the other node rejects. Mitigation for this wave: chat_scope_seq allocation is already atomic, RotateScopeKey is a single transaction, and a failed rotate retries once before giving up. Documented as a single-node-supported deployment; the existing Redis pub/sub is the natural carrier for a rotation notice when multi-node is actually built.",
    "THE scopeKeyStore.put SPLIT IS A PREREQUISITE, NOT A CLEANUP. client/e2e.go:75-83 sets latest[scope] = id unconditionally; installing an archival generation from a history page would push latest backwards and every subsequent send would be rejected with 'stale chat key for channel' (handlers.go:848). If the put/putGen split is dropped, history backfill breaks live chat. It gets its own regression test.",
    "MANDATORY TLS ON THE FILE-TRANSFER PORT breaks any client that has not been rebuilt and any operator with a TCP proxy or firewall rule inspecting that port. Mitigation: the port is dialled only by our own in-repo client, the certificate and fingerprint are identical to the control channel (no new trust decision), FileTransferInitResponse.TLS makes the failure legible instead of a hung handshake, and file_tls_enabled=false is a dev-only escape hatch that logs 'PLAINTEXT!'.",
    "ATTACHMENT DEDUP AND EXPIRING LINKS (267) STOP WORKING for chat attachments; content-addressed names also mean identical files never share storage. Mitigation: CreateLink refuses .vcx with a specific message naming the reason, the file browser disables download for encrypted rows with an explanatory tooltip, and non-chat file-browser uploads keep both features.",
    "DM ATTACHMENTS REMAIN BROKEN and this design does not fix them. Upload uses channel 0 for DMs while download uses the viewer's current channel (chat-ui.js ~1013 vs ~294, and normalize leaves channelID null for DMs), so sender and recipient disagree about where the blob lives. Encrypting a path that does not deliver would let a real defect hide behind a security change. Logged as a separate defect for its own fix.",
    "VERSION SKEW IS VISIBLE, NEVER SILENTLY WRONG. New server + old client shows blank history bodies (it does not know body_enc) and no MOTD; new client + old server refuses plaintext history by design and shows '[refused: server sent plaintext history]'. Both are legible, client and server ship from the same repo, and a one-time toast explains the mismatch.",
    "SERVERQUERY INGRESS IS STILL PLAINTEXT (internal/query/server.go:90, bare net.Listen, no TLS in the package). sendtextmessage crosses that socket in the clear before this design seals the broadcast. Explicitly out of scope for this wave; the README records it as an open item and recommends binding the query port to 127.0.0.1 until it is fixed. Other known-unfixed plaintext user text: Poke.Message, SetStatus.Message, Complaint.Reason, channel topics/descriptions, virtualserver_welcomemessage, and attachment metadata (name, size, uploader, channel, timestamp). Listed rather than half-fixed."
  ],

  "test_plan": "## Two structural guarantees worth more than any test\n\n- `store.ChatMessage.Body` is **deleted** from the struct (replaced by `BodyEnc string` + `KeyID uint32`). Every stale reader fails to compile. This is the enforcement mechanism, not a style choice.\n- `CHECK (body = '')` on `chat_messages`. Code that writes plaintext errors at the database, in production, not just in CI. `SELECT count(*) FROM chat_messages WHERE body <> ''` is a one-line ops audit that is provably always 0 after the backfill.\n\n## The canary tests must reflect, not marshal\n\nDesign A's \"keystone\" test — `json.Marshal` the `fakeChat` and assert the canary is absent — **passes vacuously**. Verified: `fakeChat` (internal/server/handlers_test.go:241-249) has every field unexported (`mu`, `nextID`, `messages`, `order`, `pins`, `reactions`, `settings`), so `json.Marshal` produces `{}` whether or not plaintext is stored. Same for `spamTracker.recent` (chatx.go:94-97). The one test the design leaned on to make the invariant self-enforcing was a tautology.\n\nThe replacement is a shared reflection helper:\n\n```go\n// internal/server/canary_test.go\n// containsCanary walks v via reflect — unexported fields included, using\n// unsafe.Pointer to read them — and reports whether the canary appears in any\n// reachable string, []byte, map key or map value.\nfunc containsCanary(t *testing.T, v any, canary string) bool\n```\n\n`TestNoPlaintextAnywhereInServerState` uses it: send \"canary-7f3a\", edit it, pin it, then walk the whole `fakeChat`, the `spamTracker`, and every captured broadcast payload. Any future code that reintroduces a plaintext write fails here without a new test being written.\n\n## internal/store (real-DB, existing skip-when-no-DB convention from store_test.go:30)\n1. `TestMigrationLedgerRunsEachFileOnce` — call `Migrate()` twice; assert `schema_migrations` has one row per file and that a sentinel row inserted between the calls survives. **This is the test that would have caught the wiped-MOTD bug in both input designs.**\n2. `TestMigrationLedgerRecordsPreExistingFiles` — against a database where 001-011 already ran, `Migrate()` records all of them and applies 012 exactly once.\n3. `TestChatMessageRowsHoldNoPlaintext` — store through the pipeline, then `SELECT body, body_enc`: `body = ''` and `body_enc` does not contain the canary.\n4. `TestPlaintextWriteRejectedByConstraint` — raw `UPDATE chat_messages SET body='leak'` errors naming `chat_messages_no_plaintext`.\n5. `TestEmptyBodyRowSurvivesMigration` — seed a row with `body=''`, `deleted_at IS NULL` (producible today: handleChatSend only length-checks, so an encrypted empty plaintext stores exactly this), run 012 + backfill, assert the migration applies and the row ends up sealed. **Design B's constraint would have aborted the migration on this row.**
6. `TestOfflineDeleteThenConstraintOrder` — seed a pre-4b spool row (`from_unique_id=''`), apply 012, assert success. Design B added the CHECK before the DELETE, which fails on any such deployment.\n7. `TestScopeKeyRoundTrip`, `TestScopeKeyIDsNeverReused` — allocate, retire, allocate again; the new id is strictly greater.\n8. `TestInsertScopeKeyConflictIsAnError` — a duplicate (scope, key_id) errors rather than silently no-opping.\n9. `TestRotateScopeKeyIsAtomic` — retire+insert in one transaction; a forced failure leaves exactly one `retired_at IS NULL` row.\n\n## internal/chatcrypto\n10. `TestKEKWrapUnwrapRoundTrip`; `TestKEKRotationUnwrapsOlderGenerations` (wrap under KEK 1, add KEK 2, old rows still unwrap, new wraps use 2).\n11. `TestMissingKEKFailsClosed` — with key rows present and no file, `LoadKEKRing(allowCreate=false)` errors; with an empty table it creates one at mode 0600.\n12. `TestKEKRejectsNon32ByteMaterial` — a passphrase-looking value is refused with the `openssl rand -base64 32` hint.\n13. `TestFingerprintIsDomainSeparated`.\n\n## internal/server\n14. **`TestChatHistoryEncryptedRoundTrip` is rewritten.** Today it asserts `m.Body == \"hello history\"` — i.e. it currently asserts the leak. It becomes: `m.Body == \"\"`, `m.BodyEnc != \"\"`, `m.KeyID > 0`, `resp.Keys` contains that generation, and unsealing `resp.Keys[0]` with the member's X25519 private key then opening `m.BodyEnc` yields the canary. The existing non-member permission-denied half is kept.\n15. `TestNoPlaintextInHistoryResponse` / `...InPinsResponse` / `...InEditBroadcast` — `json.Marshal(resp)` must not contain the canary.\n16. **`TestChatPinsRequiresMembership`** — a client in channel 2 requesting pins for channel 1 gets `errCodePermissionDenied`. Verified as absent today (chatx.go:594-616 has no gate at all); this is a new access-control test, not a regression guard.\n17. `TestHistoryRefusedWithoutPublishedKey` — a client that never published an X25519 key gets `errCodePermissionDenied`, not undecryptable rows.\n18. **`TestScopeKeyNotMintedByChatSend`** — send `chat_send{channel_id: 999999, enc: true, key_id: 1}` from a client that is not in channel 999999; assert `errCodePermissionDenied`, and assert the fake `ScopeKeyStore` recorded **zero** inserts. This is the disk-exhaustion DoS guard and it is the highest-value new test in the plan.\n19. **`TestRotateOnUnloadedScopeDoesNotCollide`** — mint gen 1 for channel 7 through the store, discard the manager's RAM, call `rotate(7)` directly, assert the new id is 2 (from `chat_scope_seq`) and that gen 1's bytes are unchanged. Guards the data-destroying bug in Design A.\n20. **`TestHistorySurvivesRestart`** — build a second `chatKeyManager` over the same store and KEK, assert `at(scope, 1)` recovers identical bytes and ciphertext written by the first manager still opens.\n21. `TestHistoryAcrossRotation` — send under gen 1, force a leave, send under gen 2; one history fetch returns both generations in `Keys` and both bodies decrypt.\n22. `TestKeyBundleTruncationIsNotRefusal` — a page spanning more than 64 generations sets `Truncated` and does NOT list the surplus in `Refused`.\n23. `TestChatKeyRequestRequiresMembership` / `TestChatKeyRequestRateLimited` / `TestChatKeyRequestRejectsEmptyIDs`.\n24. `TestRotationCoalescing` — ten leaves inside the window produce exactly one new generation.\n25. **`TestPlaintextEscapeHatchStillStoresAndRelaysCiphertext`** — `cfg.ChatAllowPlaintext = true`, send `Enc:false`; assert the stored row is ciphertext AND the broadcast carries `Enc:true` with a real `key_id`. This is the branch where the server is the encryptor and it is the easiest one to leave un-exercised.\n26. `TestStorageFailureIsNotRelayed` — a store that returns an error produces `errCodeUnavailable` and **zero** broadcasts.\n27. `TestEditRejectsStaleKeyID` / `TestEditStoresCiphertextVerbatim`.\n28. `TestSpamTrackerRetainsNoPlaintext` — feed the canary, walk `recent` reflectively, assert absence, and assert the third identical message in 30s still trips (116 unchanged).\n29. `TestSpamTrackerStripsAttachmentKeys` — three pastes of the same image with different random file keys still trip the heuristic.\n30. **`TestChatPipelineLogsNoBodies`** — run the full send path under `zaptest/observer`, walk every entry's message and every field's `String`/`Interface`, assert the canary appears nowhere. Catches a future `zap.String(\"body\", plain)` forever.\n31. `TestMOTDSealedInAuthResponse` — replaces `TestMOTDInAuthResponse`, which currently asserts the leak. Assert `MOTDEnc`, `MOTDKeyID > 0`, `MOTD != plaintext`, `ChatKeys` contains the global generation, and that unsealing `ChatKeys[0]` then opening `MOTD` yields the canary. Also assert a client sending no X25519 key gets an empty MOTD.\n32. `TestAnnouncementSealed` — the event has `enc:true`, a `key_id` equal to the global generation, and unseals correctly. Assert the auth-time replay still reaches a client that never publishes a key (guards against Design B's regression).\n33. `TestServerTextSealed` — `SendServerText` in all three target modes produces `Enc:true` broadcasts opening with the appropriate scope key.\n34. `TestServerInfoMOTDPlaintextIsDeliberate` — documents the exception, and `server_info_motd=false` omits it.\n35. `TestLegacyBackfillEncryptsAndBlanks` — seed pre-012 plaintext, run `EncryptLegacyChatHistory(ctx, \"encrypt\")`, assert `CountPlaintextBodies() == 0`, every row decrypts to its original text, both constraints validated, marker set, and a second run is a no-op. `TestLegacyBackfillPurgeMode` leaves zero plaintext and zero ciphertext.\n\n## Moderation regression guard — the real invariant\n36. `TestSlowMode`, `TestChatRateLimit`, `TestChatSpam`, `TestWordAndLinkFilters`, `TestMentions`, `TestChatDeleteAny` must all still pass.\n  **Design B's framing — 'not one moderation test should need editing' — is unachievable and must not be the constraint's proof.** `newChatKeyManager()` takes no arguments today (chatkeys.go:43, wired at tcp.go:247) and now takes `(ScopeKeyStore, *chatcrypto.KEKRing, *zap.Logger)`; every one of those tests reaches `sealFor -> current()` through `chatPair`/`readChannelKeyFor`, so the scaffolding MUST change or every send fails with `errCodeUnavailable`. The honest invariant is asserted directly instead: `TestModerationPipelineOrderUnchanged` uses a recording stub to assert that `moderateBody`, `spamTracker.record` and `parseMentions` are called with the same arguments, in the same order, on the same decrypted body, before storage — and that none of them ever reads from the store.\n\n## internal/filetransfer\n37. `TestFileTransferRequiresTLS` — a bare `net.Dial` cannot complete a transfer against a TLS listener.\n38. `TestFileTransferTLSRoundTrip` — upload and download succeed through `tls.Client` with fingerprint pinning; `TestFingerprintMismatchRejected`.\n39. `TestCreateLinkRefusesEncryptedAttachment` — `.vcx` names are refused with the specific message.\n\n## client\n40. `TestChatHistoryStripsCiphertextBeforeReturn` — every returned entry has `BodyEnc == \"\"`, `KeyID == 0`, `Body` decrypted, `EncVerified == true`. The webview-boundary invariant.\n41. **`TestArchivalKeyDoesNotDowngradeLatest`** — `put(scope, 7, k)` then `putGen(scope, 3, k)`; `current()` still reports 7. Guards the landmine directly.\n42. `TestRefuseServerPlaintextHistory` — an entry with `Body` set and `BodyEnc` empty becomes `refusedPlainText` with `EncVerified == false`.\n43. `TestMissingVsRefusedPlaceholders` — the two placeholders are selected correctly from `Refused` vs absence.\n44. `TestMOTDDecryptedBeforeConnectReturns` — `App.MOTD()` immediately after `Connect()` returns the plaintext, with no sleep and no event wait. **This is the test Design B's approach would fail.**\n45. `TestAttachmentSealRoundTrip`; `TestFileTokenParse` — `[file:abc.vcx#KEY#photo.png]` parses into three parts, a legacy `[file:photo.png]` yields an empty key, and a malformed single-`#` capture degrades to plain rather than throwing.\n46. `TestAttachmentStorageNameIsContentDerived` — two uploads of the same display name produce different storage names, and the first blob is untouched.\n47. `TestChatSearchDecryptsPages` — finds a canary that exists only as ciphertext server-side, and reports `Undecryptable` for an unobtainable generation.\n48. `TestExportChatEncryptedRoundTrip`.\n\n## cmd/e2e/main.go\n49. The black-box harness is updated and must stay green: `X25519PublicKey` at auth, `AuthResponse.ChatKeys` unsealing, piggybacked `Keys` handling, and `openScope` before the `m.Body == text` (:991) and `m.Body != \"\"` (:1077) assertions. Neither input design mentioned the one binary that validates the real protocol end to end.\n\n## Whole-repo sweep\n50. `TestNoPlaintextCanaryAnywhere` (`-tags integration`): boot server + store + filetransfer, push the canary through global chat, channel chat, an edit, a pin, an attachment, a MOTD and an announcement; then sweep every column of every table with `::text LIKE '%canary%'`, every file under `FileRoot`, and the captured log buffer. One test, one assertion, and it is the one that would have caught all thirteen paths in the original audit plus the two it missed.\n\n## Manual checklist (no frontend test harness exists)\nFresh install AND upgrade-from-plaintext, both: scroll back past a rotation; open pins as a member and confirm a non-member is refused; run a search and confirm the not-decryptable counter; paste the SAME filename twice into one channel and confirm both messages still render their own image; paste into a channel and reopen after a restart; edit a message and confirm it renders without a history refetch; dismiss an announcement and reconnect; confirm the MOTD appears on first paint; export a chat both ways; confirm every shield/lock tooltip and that the MOTD banner carries neither.",

  "work_packets": [
    {
      "name": "P1 — CONTRACT: protocol, schema, store, KEK",
      "order": "FIRST. P2, P3 and P4 code against the contracts stated here without seeing this work in progress. Every signature, column, constant and JSON tag below is frozen; P1 must implement exactly this and nothing more.",
      "owns_exclusively": [
        "internal/netproto/codec.go",
        "internal/store/store.go",
        "internal/store/chat.go",
        "internal/store/chatkeys.go (new)",
        "internal/store/migrations/012_chat_encryption.sql (new)",
        "internal/chatcrypto/chatcrypto.go (new)",
        "internal/chatcrypto/chatcrypto_test.go (new)",
        "internal/store/chat_test.go",
        "internal/store/store_test.go"
      ],
      "must_implement": [
        "MIGRATION LEDGER in store.go Migrate(): create schema_migrations(filename TEXT PRIMARY KEY, applied_at TIMESTAMPTZ NOT NULL DEFAULT NOW()); skip already-recorded files; apply each remaining file and INSERT its ledger row in ONE explicit transaction. This must land before anything else in this packet.",
        "012_chat_encryption.sql exactly as written in schema_changes, in that statement order: chat_scope_keys + uq_chat_scope_keys_current, chat_scope_seq, chat_messages ADD body_enc/key_id, server_settings ADD key_id, files ADD encrypted, DELETE FROM offline_messages, then the three CHECK constraints (chat_messages_no_plaintext NOT VALID, chat_messages_sealed NOT VALID, offline_messages_sealed valid). The DELETE MUST precede offline_messages_sealed.",
        "internal/chatcrypto: KEKRing, LoadKEKRing(path, envB64 string, allowCreate bool) (*KEKRing, error), Wrap(key [32]byte) (uint16, []byte, error), Unwrap(kekID uint16, wrapped []byte) ([32]byte, error), NewestID() uint16, Fingerprint() string. Wrapped form nonce[24]||secretbox(key32, KEK). File format '<id>:<base64 32 raw bytes>' per line, mode 0600. Reject anything that is not 32 raw bytes. No passphrase mode. Fingerprint = hex(sha256(\"voicx-kek-fp-v1\"||newestKey))[:16].",
        "store/chat.go: DELETE ChatMessage.Body; ADD BodyEnc string and KeyID uint32. StoreChatMessage(ctx, channelID int64, fromUniqueID, fromNickname, bodyEnc string, keyID uint32) (int64, error). EditChatMessage(ctx, id int64, bodyEnc string, keyID uint32) error. DeleteChatMessage clears body_enc and key_id too. ChatHistory/GetChatMessage select body_enc, key_id. SetServerSetting(ctx, key, value string, keyID uint32) error. GetServerSetting(ctx, key string) (string, uint32, error).",
        "store/chat.go backfill helpers: LegacyPlaintextPage(ctx, afterID int64, limit int) ([]LegacyChatRow, error) selecting WHERE body_enc = '' AND deleted_at IS NULL ORDER BY id; SetChatCiphertext(ctx, id int64, bodyEnc string, keyID uint32) error; PurgeLegacyPlaintext(ctx) (int64, error); CountPlaintextBodies(ctx) (int64, error); ValidateChatNoPlaintext(ctx) error (VALIDATEs BOTH constraints).",
        "store/chatkeys.go: ScopeKey struct {KeyID uint32; Wrapped []byte; KEKID uint16; CreatedAt time.Time; RetiredAt *time.Time}; CountScopeKeys; AllocScopeKeyID (the chat_scope_seq upsert returning next_key_id-1); CurrentScopeKey (nil,nil when none); GetScopeKey; InsertScopeKey (HARD ERROR on PK conflict, never ON CONFLICT DO NOTHING); RotateScopeKey (retire-then-insert in ONE transaction).",
        "codec.go: MsgChatKeyRequest = 99, MsgChatKeyBundle = 100, their String() cases and Decode registry cases. ChatKeyRequest{ChannelID int64 `json:\"channel_id\"`; KeyIDs []uint32 `json:\"key_ids\"`}. ChatKeyBundle{ChannelID int64 `json:\"channel_id\"`; Keys []ChannelKey `json:\"keys\"`; Refused []uint32 `json:\"refused,omitempty\"`; Truncated bool `json:\"truncated,omitempty\"`}.",
        "codec.go ChatHistoryEntry: Body gets `json:\"body,omitempty\"`; ADD BodyEnc string `json:\"body_enc,omitempty\"`, KeyID uint32 `json:\"key_id,omitempty\"`, EncVerified bool `json:\"enc_verified,omitempty\"`. ChatHistoryResponse and ChatPinsResponse each ADD Keys []ChannelKey `json:\"keys,omitempty\"`, Refused []uint32 `json:\"refused,omitempty\"`, Truncated bool `json:\"truncated,omitempty\"`.",
        "codec.go: Authenticate and AuthSignature each ADD X25519PublicKey string `json:\"x25519_public_key,omitempty\"`. AuthResponse ADDs MOTDEnc bool `json:\"motd_enc,omitempty\"`, MOTDKeyID uint32 `json:\"motd_key_id,omitempty\"`, ChatKeys []ChannelKey `json:\"chat_keys,omitempty\"` (MOTD string itself is unchanged in name and tag). FileTransferInitResponse ADDs TLS bool `json:\"tls,omitempty\"`, TLSFingerprint string `json:\"tls_fingerprint,omitempty\"`.",
        "Doc comments: store/chat.go's header must stop saying 'Bodies are stored decrypted'. codec.go's ChatHistoryEntry must carry the 'server populates only BodyEnc/KeyID; Body is always empty on the wire; EncVerified is client-set' contract verbatim.",
        "Tests 1-13 and 35 from the test plan."
      ],
      "must_not_touch": [
        "Anything under internal/server/ — P2 owns the ChatStore and ScopeKeyStore interface declarations in deps.go and will write them to match these signatures.",
        "Anything under internal/filetransfer/ — P3 owns it and consumes only the FileTransferInitResponse fields defined here.",
        "Anything under client/ or cmd/ — P4 and P2 own those.",
        "internal/config/config.go — P2 owns it.",
        "Do NOT add MsgServerMessage or any third message type. Do NOT add a body_len column. Do NOT add chat_scope_key_grants (chat_history_scope=since_join is deliberately not built). Do NOT blank server_settings values in SQL — P2's Go backfill seals them in place. Do NOT drop chat_messages.body (deferred to 013)."
      ]
    },
    {
      "name": "P2 — SERVER: key manager, chat pipeline, backfill, wiring",
      "order": "SECOND. Codes against P1's frozen contract. Owns every file under internal/server/ plus config and the server binary.",
      "owns_exclusively": [
        "internal/server/chatkeys.go",
        "internal/server/chatx.go",
        "internal/server/e2e.go",
        "internal/server/handlers.go",
        "internal/server/admin.go",
        "internal/server/presence.go",
        "internal/server/deps.go",
        "internal/server/tcp.go",
        "internal/server/files.go",
        "internal/server/chatmigrate.go (new)",
        "internal/server/canary_test.go (new)",
        "all internal/server/*_test.go",
        "internal/config/config.go",
        "cmd/server/main.go"
      ],
      "must_implement": [
        "chatkeys.go rewritten: the two-level lock (manager mu guards the scopes map ONLY and is never held across DB I/O; each *scopeEntry has its own mutex under which DB calls happen). EnsureScope is the ONLY minting entry point; current() never mints and returns ErrNoScopeKey; at(ctx, scope, gen); rotate uses store.AllocScopeKeyID and store.RotateScopeKey and NEVER derives an id from RAM; sealFor/open/seal take an explicit generation. crypto/rand failures return errors — delete the zero-key path at :55-59 and the ignored nonce error at :136. Rewrite the file header (the 'keys live in memory only' paragraph is now false) and state the entitlement rule: a current member may fetch every generation; rotation is not forward secrecy over history.",
        "Rotation coalescing per chat_key_rotate_min_seconds (default 60): a rotate request inside the window sets a per-scope dirty flag and a timer that fires one rotation covering all leaves in the window. Generations are NEVER deleted, only retired. Comment the invariant at both ends of rotateScopeKey that redistribution happens strictly AFTER rotate returns, because deliverScopeKey -> sealFor -> current re-enters the scope entry mutex.",
        "handleChatSend: add the membership check (State.GetClient(client.ID).ChannelID == parsed channelID) BEFORE touching chatKeys; then the non-minting current(); ErrNoScopeKey -> errCodeUnavailable 'no chat key for this channel yet — rejoin the channel'.",
        "routeScopedChat: store msg.Text/msg.KeyID verbatim when msg.Enc; seal server-side when !msg.Enc; ALWAYS broadcast with Enc:true and the stored keyID (closes the chat_allow_plaintext relay hole); make a storage error FATAL to the send (errCodeUnavailable, no relay) instead of the current Warn-and-relay at :223-228.",
        "stripAttachmentRefs(body string) string rewriting [file:<storage>#<key>#<display>] to [file:<display>], applied before moderateBody and before spamTracker.record.",
        "spamTracker: spamEntry.body string -> sum [32]byte; record(uid string, sum [32]byte, now time.Time) bool. Callers assign sha256.Sum256(...) to a TEMPORARY before slicing — sha256.Sum256(x)[:n] does not compile.",
        "chatHistoryEntry: set BodyEnc/KeyID, never Body; tombstones clear both.",
        "handleChatHistory: keep the membership check, ADD the published-X25519-key gate, ADD key-bundle assembly capped at maxKeysPerResponse = 64 with Truncated set on overflow and Refused only for genuinely withheld generations.",
        "handleChatPins: ADD the membership check it currently lacks entirely (scopeReadable), the published-key gate, and the same key-bundle assembly.",
        "handleChatEdit: ADD msg.KeyID == current validation; store NewText/KeyID verbatim; broadcast NewText as-is (the re-seal at :472-474 becomes dead for the encrypted path).",
        "NEW handleChatKeyRequest for type 99: reject empty KeyIDs, cap 64, chatRate.allow gate, scopeReadable gate, sealFor per generation, unknown/ungranted -> Refused.",
        "handleAuthenticate: read X25519PublicKey from Authenticate (password path) and AuthSignature (challenge path), SetE2EPublicKey immediately, populate AuthResponse.ChatKeys with the sealed global generation and MOTD/MOTDEnc/MOTDKeyID with the sealed MOTD. LEAVE the announcement replay at :428-434 exactly where it is.",
        "deliverSpooled: delete the from_unique_id=='' plaintext branch; Enc/E2E unconditionally true.",
        "SetServerSettingAndAnnounce seals under the global generation and broadcasts {text, enc:true, key_id}. NEW serverSettingSealed re-seals under the current generation when the stored key_id is retired.",
        "admin.go SendServerText: seal all three target modes (channel key for mode 2, global for 1 and 3), Enc:true + KeyID. Doc comment states that mode 1 is a server notice, not an E2EE DM.",
        "presence.go handleServerInfoQuery: keep MOTD plaintext, gated by the new server_info_motd config.",
        "deps.go: update ChatStore to P1's signatures; ADD ScopeKeyStore interface (CountScopeKeys, AllocScopeKeyID, CurrentScopeKey, GetScopeKey, InsertScopeKey, RotateScopeKey) and Deps.ScopeKeys.",
        "tcp.go: dispatch MsgChatKeyRequest; NewTCPServer takes *chatcrypto.KEKRing and builds newChatKeyManager(deps.ScopeKeys, kek, logger); context through to rotateScopeKey at :634.",
        "chatmigrate.go EncryptLegacyChatHistory(ctx, mode) per schema_changes, including sealing server_settings motd/announcement in place, the double VALIDATE, the marker, and the WARN about dead tuples/WAL/base backups.",
        "config.go: chat_master_key_file (./data/keys/chat_master.key), chat_legacy_history (encrypt), chat_key_rotate_min_seconds (60), chat_search_max_messages (2000), file_tls_enabled (true), server_info_motd (true). Rewrite the chat_allow_plaintext docstring to 'permits unencrypted SENDS only; never affects storage, relay, history or MOTD'.",
        "cmd/server/main.go: boot order store.Migrate -> CountScopeKeys -> chatcrypto.LoadKEKRing(file, env, count==0) -> log fingerprint + backup warning -> NewTCPServer -> EnsureGlobalScopeKey -> EncryptLegacyChatHistory -> filetransfer (TLS material from the SAME tlscert.Ensure call the control channel uses, per P3's Config contract) -> tcp.Start. All fatal on error. Add rewrap-chat-keys and --reset-chat-keys subcommands.",
        "canary_test.go containsCanary reflection helper (unexported fields included), and tests 14-34 and 36 from the test plan."
      ],
      "must_not_touch": [
        "internal/netproto/codec.go, internal/store/**, internal/chatcrypto/** — P1 owns them. Code against the frozen signatures; if something is missing, it is a contract bug to be raised, not patched locally.",
        "internal/filetransfer/** — P3 owns it. P2 only constructs filetransfer.Config{TLSEnabled, Cert, Fingerprint} using the field names P3 publishes below.",
        "client/** and cmd/e2e/** — P4 owns them.",
        "Do NOT move the announcement replay out of handleAuthenticate. Do NOT add a MsgServerMessage handler. Do NOT implement chat_history_scope/since_join. Do NOT delete retired scope key generations under any condition."
      ]
    },
    {
      "name": "P3 — FILE TRANSFER: TLS on the data port",
      "order": "PARALLEL with P2 and P4. Fully independent; touches one package. Its Config field names are the contract P2 wires against and are frozen here.",
      "owns_exclusively": [
        "internal/filetransfer/server.go",
        "internal/filetransfer/transfer.go",
        "internal/filetransfer/links.go",
        "internal/filetransfer/*_test.go"
      ],
      "must_implement": [
        "CONTRACT FOR P2 — filetransfer.Config gains exactly these three fields, spelled exactly so: TLSEnabled bool; Cert tls.Certificate; Fingerprint string. Server gains method Fingerprint() string returning cfg.Fingerprint.",
        "Start(): after the existing net.Listen, wrap when TLSEnabled — ln = tls.NewListener(ln, &tls.Config{Certificates: []tls.Certificate{s.cfg.Cert}, MinVersion: tls.VersionTLS12}). This is the only line at :438 that changes. When TLSEnabled is false, log 'file transfer listener started (PLAINTEXT!)' at Warn.",
        "CreateLink (links.go): refuse names ending in .vcx with errors.New(\"encrypted chat attachment — open it in the client\"), because /dl/<token> on the health port has no key and would serve ciphertext that looks like corruption.",
        "finalizeUpload / the files row writer: accept and persist the encrypted BOOLEAN flag added by migration 012 for uploads whose name ends in .vcx. Do NOT add version rotation logic for them — content-addressed names never collide, so the .v1..v3 path is simply never reached for chat attachments.",
        "Tests 37-39 from the test plan: TestFileTransferRequiresTLS, TestFileTransferTLSRoundTrip, TestFingerprintMismatchRejected, TestCreateLinkRefusesEncryptedAttachment."
      ],
      "must_not_touch": [
        "Everything outside internal/filetransfer/. In particular do NOT edit cmd/server/main.go to wire the TLS material — P2 does that against the Config contract above.",
        "Do NOT add encryption, key handling or .vcx awareness beyond the two places named (CreateLink refusal, files.encrypted flag). The package moves opaque bytes and that is deliberate — all attachment crypto lives in P4's client code.",
        "internal/netproto/codec.go — P1 owns the FileTransferInitResponse.TLS/TLSFingerprint fields; P2 populates them."
      ]
    },
    {
      "name": "P4 — CLIENT: Go backend, frontend, e2e harness",
      "order": "PARALLEL with P2 and P3. Codes against P1's frozen protocol contract only; needs nothing from P2 or P3 at compile time.",
      "owns_exclusively": [
        "client/e2e.go",
        "client/chat.go",
        "client/conn.go",
        "client/app.go",
        "client/identity.go",
        "client/*_test.go",
        "client/frontend/src/chat-ui.js",
        "client/frontend/src/files-ui.js",
        "client/frontend/src/main.js",
        "cmd/e2e/main.go"
      ],
      "must_implement": [
        "e2e.go: SPLIT scopeKeyStore.put into put (advances latest only when id > latest[scope]) and putGen (never touches latest); add has(). This is a prerequisite — without it, backfilling an archival generation breaks live sending with 'stale chat key for channel'.",
        "e2e.go: installKeys(scope, []netproto.ChannelKey) (box.OpenAnonymous + putGen); requestKeys(scope, ids) writing MsgChatKeyRequest; the three placeholder constants missingKeyText / refusedKeyText / refusedPlainText.",
        "e2e.go: maybeDecryptChat -> maybeDecryptEvent, keeping the chat branch verbatim and adding an announcement branch (unseal data.text with scopeKeys.get(0, data.key_id)) and a chat_edited branch (unseal data.body). The missing-key path spawns go guardCrash(\"chatKeyFetch\", ...) — it MUST be a goroutine because maybeDecryptEvent runs on the read loop and connManager.request holds the process-global reqMu waiting for a reply that arrives on that same loop; and it MUST have its own guardCrash because recover is per-goroutine and this parses attacker-controlled ciphertext (item 331).",
        "e2e.go: sealFile/openFile ([]byte, nonce[24]||secretbox, no base64).",
        "chat.go ChatHistory and ChatPins: installKeys, then per entry — refuse (BodyEnc==\"\" && Body!=\"\") with refusedPlainText, select refusedKeyText vs missingKeyText from Refused vs absence, open otherwise and set EncVerified, then ALWAYS clear BodyEnc and KeyID before returning to the webview.",
        "chat.go: ChatSearch(channelID int64, query string, maxMessages int) (ChatSearchResult, error) paging ChatHistory at 200/page, counting Undecryptable via !EncVerified, emitting chatsearch:progress.",
        "chat.go: UploadChatAttachment(channelID int64, name, dataBase64 string) (string, error) returning the token [file:<storage>#<keyB64>#<display>] where storage = hex(sha256(ciphertext))[:32]+\".vcx\" and the display name has ] and # replaced with _. DownloadChatAttachment(channelID int64, storage, keyB64 string) (string, error), empty key = plain download. BOTH return (T, error) — never (string, string).",
        "chat.go: ftDial(addr, fingerprint) via tls.DialWithDialer with InsecureSkipVerify plus a VerifyPeerCertificate fingerprint pin, taking the value from FileTransferInitResponse.TLSFingerprint and cross-checking it against the control-channel pin at conn.go:141. Delete the 'plaintext this wave' comment at :245-246.",
        "chat.go: ExportChatEncrypted(defaultName, contents, passphrase string) (string, error) writing magic||argon2id-salt||nonce||secretbox as .voicxchat at 0600.",
        "conn.go: send X25519PublicKey on BOTH Authenticate (password path) and AuthSignature (guest/challenge path — the guest path leaves Authenticate.PublicKey empty). In Connect(), install resp.ChatKeys via put and decrypt resp.MOTD into m.motd BEFORE returning. Add the MsgChatKeyBundle read-loop case routing to installKeys.",
        "chat-ui.js normalize: enc: !!d.enc || !!d.enc_verified. renderMsg: rewritten shield tooltip, new missing-key/⚠ state for the three placeholders.",
        "chat-ui.js renderBody: the split regex KEEPS EXACTLY ONE CAPTURE GROUP (/\\[file:([^\\]]+)\\]/) and BOTH loops stay at i += 2. Add parseFileRef(cap) doing two indexOf(\"#\") lookups and degrading to plain on 0 or 1 separators. attachFileRef consumes the raw capture, calls parseFileRef, displays ref.name, and calls DownloadChatAttachment(chID, ref.storage, ref.key). The key must never reach textContent, an attribute or a src.",
        "chat-ui.js: attachment send via UploadChatAttachment; searchServer replaced by one ChatSearch call with the 'N matches (M could not be decrypted)' header; exportChat confirm + encrypted option; onConnect MOTD rendered as 'server notice — …' system banner; DELETE the chat_edited history-refetch workaround at :948-961.",
        "files-ui.js: encrypted rows render '🔒 chat attachment' with download disabled and the explanatory tooltip.",
        "cmd/e2e/main.go: X25519PublicKey at auth, AuthResponse.ChatKeys unsealing, piggybacked Keys handling, openScope before the m.Body assertions at :991 and :1077.",
        "Tests 40-48 from the test plan."
      ],
      "must_not_touch": [
        "internal/** in its entirety. P4 consumes netproto structs read-only; if a field is missing it is a P1 contract bug.",
        "Do NOT change the [file:...] regex to add a second capture group under any circumstance — split() interleaves captures at stride 1+captureCount and the loops would leak the base64 file key into innerHTML.",
        "Do NOT add a MOTD event listener or re-render path; the MOTD is resolved synchronously inside Connect() and App.MOTD()/motdSnapshot()/onConnect() stay exactly as they are.",
        "Do NOT attempt to fix the DM attachment channel-id mismatch (upload uses 0, download uses the viewer's channel) — it is a separate logged defect and bundling it would hide a real bug behind a security change."
      ]
    }
  ]
}
```